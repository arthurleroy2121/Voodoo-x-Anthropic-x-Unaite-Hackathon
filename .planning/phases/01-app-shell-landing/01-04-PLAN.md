---
phase: 01-app-shell-landing
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/project/page.tsx
  - components/landing/Hero.tsx
  - components/landing/WorkflowCards.tsx
  - components/landing/LandingCTA.tsx
  - components/layout/AppShell.tsx
  - components/layout/Sidebar.tsx
  - components/layout/TopBar.tsx
  - components/layout/WorkflowTabs.tsx
autonomous: true
requirements:
  - REQ-landing-page
  - REQ-file-architecture
user_setup: []
must_haves:
  truths:
    - "La page `/` affiche le titre `Voodoo Creative Radar`, la tagline `From Market Signals to Testable Creatives`, le paragraphe descriptif PRD et les 4 cartes de workflow numérotées"
    - "Le bouton `Get Started` sur `/` route vers `/project`"
    - "La page `/project` affiche le header `Demo Project — Voodoo Creative Radar`"
    - "La sidebar affiche `Voodoo Creative Radar`, group `Projects`, `• Demo Project` actif, `+ New Project` grisé"
    - "Les 4 onglets `1. Game Identity` / `2. Market Scan` / `3. Pattern Analysis` / `4. Creative Output` sont navigables manuellement (state local Phase 1, branché sur useApp en Phase 2)"
    - "UI premium light : fond off-white, charcoal text, single accent magenta `#E91E63`, no dark mode, no JSON brut"
    - "pnpm dev démarre sans erreur d'hydration ; pnpm build passe"
  artifacts:
    - path: app/layout.tsx
      provides: "Root layout avec Geist Sans font + globals.css import + metadata"
      contains: "GeistSans"
    - path: app/page.tsx
      provides: "Landing page server component composée de Hero + WorkflowCards + LandingCTA"
      contains: "LandingCTA"
    - path: app/project/page.tsx
      provides: "Page projet shell visuel — AppShell + Sidebar + TopBar + WorkflowTabs"
      contains: "WorkflowTabs"
    - path: components/landing/Hero.tsx
      provides: "Section héro : titre Voodoo Creative Radar + tagline + paragraphe descriptif"
      contains: "Voodoo Creative Radar"
    - path: components/landing/WorkflowCards.tsx
      provides: "Stepper horizontal 4 cartes numérotées 01-04 avec ligne fine connectrice"
      contains: "Select a game"
    - path: components/landing/LandingCTA.tsx
      provides: "Bouton Get Started → /project"
      contains: "Get Started"
    - path: components/layout/AppShell.tsx
      provides: "Container layout pour /project avec slot enfant"
      contains: "min-h-screen"
    - path: components/layout/Sidebar.tsx
      provides: "Sidebar gauche statique avec Voodoo Creative Radar + Projects + Demo Project + + New Project (disabled)"
      contains: "+ New Project"
    - path: components/layout/TopBar.tsx
      provides: "Header projet avec titre `Demo Project — Voodoo Creative Radar`"
      contains: "Demo Project"
    - path: components/layout/WorkflowTabs.tsx
      provides: "Tabs 4 onglets numérotés client component, state local Phase 1"
      contains: "'use client'"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/{Hero,WorkflowCards,LandingCTA}.tsx"
      via: "import"
      pattern: "from '@/components/landing"
    - from: "app/project/page.tsx"
      to: "components/layout/{AppShell,Sidebar,TopBar,WorkflowTabs}.tsx"
      via: "import"
      pattern: "from '@/components/layout"
    - from: "components/landing/LandingCTA.tsx"
      to: "/project route"
      via: "next/link href=\"/project\""
      pattern: "href=\"/project\""
    - from: "app/layout.tsx"
      to: "next/font Geist Sans"
      via: "import { GeistSans } from 'geist/font/sans'"
      pattern: "GeistSans"
---

<objective>
Livrer le **livrable utilisateur visible** de la Phase 1 : la landing page premium `/` avec le contenu PRD strict + le shell visuel `/project` avec sidebar, top bar et 4 onglets navigables. Tout consomme les fondations posées par les Plans 01 (tokens design system, layout root), 02 (types, copy si besoin) et 03 (atomes UI Button, Card, Badge, Tabs).

Le shell `/project` reçoit un placeholder statique par tab (`Étape X — à venir`) ; la branchement Zustand est livré en **Phase 2** (D-17).

Purpose: Sans ce plan, la Phase 1 ne livre pas le user-facing. C'est le seul plan de la Wave 3 et le seul à toucher `app/page.tsx` + `app/project/page.tsx` + `app/layout.tsx`.
Output: Landing publique fonctionnelle (CTA → /project) + shell `/project` avec tabs navigables + Geist Sans correctement câblé via next/font.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-landing/01-CONTEXT.md
@.planning/phases/01-app-shell-landing/01-PATTERNS.md
@.planning/research/PITFALLS.md
@voodoo-creative-radar-prd.md
@.planning/phases/01-app-shell-landing/01-01-SUMMARY.md
@.planning/phases/01-app-shell-landing/01-02-SUMMARY.md
@.planning/phases/01-app-shell-landing/01-03-SUMMARY.md

<interfaces>
<!-- API atomes UI Plan 03 (consommés ici) -->
import { Button } from '@/components/ui/Button';
  // <Button variant="primary"|"ghost"|"outline" size="sm"|"md"|"lg" />
import { Card } from '@/components/ui/Card';
  // <Card title?="..." description?="..."> {children} </Card>
import { Tabs } from '@/components/ui/Tabs';
  // <Tabs tabs={readonly Tab[]} activeTab={string} onTabChange={(id: string) => void} />

<!-- API store Zustand Plan 02 (PAS branché en Phase 1, juste à connaître pour la Phase 2) -->
import { useApp } from '@/lib/state';
  // Phase 2 fera : const currentStep = useApp((s) => s.currentStep); const setStep = useApp((s) => s.setStep);

<!-- Tokens design (rappel depuis Plan 01) -->
--color-bg #fafafa
--color-surface #ffffff
--color-charcoal #1a1a1a
--color-muted #6b7280
--color-border #e5e5e5
--color-accent #E91E63

<!-- Contenu PRD strict (verbatim) -->
Title: "Voodoo Creative Radar"
Tagline: "From Market Signals to Testable Creatives"
Description: "Analyse market ads, extract winning creative patterns, and generate a testable 30-second ad for a selected Voodoo game."
Workflow cards (4): "1. Select a game" / "2. Scan the market" / "3. Analyze winning patterns" / "4. Generate a creative"
CTA: "Get Started" → /project
Project header: "Demo Project — Voodoo Creative Radar"
Sidebar : "Voodoo Creative Radar" / Projects / "• Demo Project" (active) / "+ New Project" (visible but disabled)
Tabs (4) : "1. Game Identity" / "2. Market Scan" / "3. Pattern Analysis" / "4. Creative Output"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: app/layout.tsx + composants landing (Hero, WorkflowCards, LandingCTA) + app/page.tsx</name>
  <read_first>
    - app/layout.tsx (généré par next-app au Plan 01)
    - app/page.tsx (généré par next-app au Plan 01 — sera totalement remplacé)
    - app/globals.css (Plan 01 — tokens @theme)
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (sections HeroSection, WorkflowStepper)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-09 Geist, D-12-D-15 landing composition)
    - voodoo-creative-radar-prd.md (lignes 46-55 : landing content)
  </read_first>
  <files>
    - app/layout.tsx
    - app/page.tsx
    - components/landing/Hero.tsx
    - components/landing/WorkflowCards.tsx
    - components/landing/LandingCTA.tsx
  </files>
  <action>
**1. `app/layout.tsx`** — root layout avec Geist Sans (D-09), metadata, lang="en", import globals.css. **Server component** (pas de 'use client'). PAS de useEffect Zustand ici (Phase 2) :

```typescript
import type { Metadata } from 'next';
import { GeistSans } from 'geist/font/sans';
import { GeistMono } from 'geist/font/mono';

import './globals.css';

export const metadata: Metadata = {
  title: 'Voodoo Creative Radar',
  description: 'From Market Signals to Testable Creatives',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${GeistSans.variable} ${GeistMono.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
```

NB sur Geist : la lib `geist` v1+ expose `GeistSans.variable` et `GeistSans.className`. On utilise `.variable` pour exposer la CSS variable `--font-geist-sans` (consommée dans `app/globals.css` `@theme` Plan 01) et appliquer `font-sans` au body.

**2. `components/landing/Hero.tsx`** — Server component, contenu PRD strict (D-14) :

```typescript
export function Hero() {
  return (
    <section className="flex flex-col items-center px-8 pt-24 pb-12 text-center">
      <h1 className="text-5xl font-semibold tracking-tight text-[--color-charcoal] md:text-6xl">
        Voodoo Creative Radar
      </h1>
      <p className="mt-4 text-xl text-[--color-muted]">
        From Market Signals to Testable Creatives
      </p>
      <p className="mt-6 max-w-2xl text-base leading-relaxed text-[--color-muted]">
        Analyse market ads, extract winning creative patterns, and generate a testable 30-second ad for a selected Voodoo game.
      </p>
    </section>
  );
}
```

**3. `components/landing/LandingCTA.tsx`** — Server component, Link Next.js → `/project` (D-13 : CTA au-dessus du stepper) :

```typescript
import Link from 'next/link';

export function LandingCTA() {
  return (
    <div className="flex justify-center px-8 pb-12">
      <Link
        href="/project"
        className="inline-flex items-center gap-2 rounded-md bg-[--color-accent] px-6 py-3 text-base font-medium text-white shadow-sm transition-colors hover:opacity-90 focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent] focus-visible:ring-offset-2"
      >
        Get Started
      </Link>
    </div>
  );
}
```

**4. `components/landing/WorkflowCards.tsx`** — Stepper horizontal numéroté `01 — 02 — 03 — 04` avec ligne fine connectrice (D-12). Server component, contenu PRD strict :

```typescript
const STEPS = [
  { number: '01', title: 'Select a game', description: 'Pick a Voodoo game and confirm its category and tags.' },
  { number: '02', title: 'Scan the market', description: 'Run a live Sensor Tower scan to retrieve top competitor ads.' },
  { number: '03', title: 'Analyze winning patterns', description: 'Use Gemini to surface the patterns behind a winning ad.' },
  { number: '04', title: 'Generate a creative', description: 'Produce a 30-second vertical ad via Scenario.' },
] as const;

export function WorkflowCards() {
  return (
    <section
      aria-label="Workflow"
      className="mx-auto w-full max-w-6xl px-8 pb-24"
    >
      <ol className="relative grid grid-cols-1 gap-8 md:grid-cols-4">
        {/* Connecting line — visible only on md+ between numbers */}
        <div
          aria-hidden="true"
          className="absolute left-0 right-0 top-5 hidden h-px bg-[--color-border] md:block"
        />
        {STEPS.map((step) => (
          <li
            key={step.number}
            className="relative flex flex-col items-start gap-3"
          >
            <span className="relative z-10 inline-flex size-10 items-center justify-center rounded-full bg-[--color-surface] font-mono text-sm font-semibold text-[--color-accent] ring-1 ring-[--color-border]">
              {step.number}
            </span>
            <h3 className="text-base font-semibold text-[--color-charcoal]">
              {step.title}
            </h3>
            <p className="text-sm leading-relaxed text-[--color-muted]">
              {step.description}
            </p>
          </li>
        ))}
      </ol>
    </section>
  );
}
```

Notes design (D-12, D-13, D-14) :
- Les numéros `01-04` sont en **magenta `#E91E63`** (single accent — D-08), monospace via `font-mono` (Geist Mono déjà chargé)
- La ligne fine connectrice horizontale est `border-[--color-border]` (gris très clair)
- Aucune autre touche de magenta dans la landing (le CTA Get Started est aussi magenta — single accent strict)
- Le contenu titre + description par étape n'est PAS dans le PRD ligne 52 (qui ne donne que les titres) — les descriptions sont **dérivées** mais doivent être de la même nature que le PRD : elles synthétisent le rôle de l'étape. Si l'executor préfère ne mettre que le titre, c'est acceptable, mais Phase 1 souhaite que la landing soit auto-explicative.

**5. `app/page.tsx`** — Compose la landing. Server component (D-13 : CTA au-dessus du stepper) :

```typescript
import { Hero } from '@/components/landing/Hero';
import { LandingCTA } from '@/components/landing/LandingCTA';
import { WorkflowCards } from '@/components/landing/WorkflowCards';

export default function LandingPage() {
  return (
    <main className="min-h-screen bg-[--color-bg]">
      <Hero />
      <LandingCTA />
      <WorkflowCards />
    </main>
  );
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q "GeistSans" app/layout.tsx && grep -q "lang=\"en\"" app/layout.tsx && grep -q "title: 'Voodoo Creative Radar'" app/layout.tsx && grep -q "Voodoo Creative Radar" components/landing/Hero.tsx && grep -q "From Market Signals to Testable Creatives" components/landing/Hero.tsx && grep -q "Analyse market ads" components/landing/Hero.tsx && grep -q "href=\"/project\"" components/landing/LandingCTA.tsx && grep -q "Get Started" components/landing/LandingCTA.tsx && grep -q "Select a game" components/landing/WorkflowCards.tsx && grep -q "Scan the market" components/landing/WorkflowCards.tsx && grep -q "Analyze winning patterns" components/landing/WorkflowCards.tsx && grep -q "Generate a creative" components/landing/WorkflowCards.tsx && grep -q "Hero" app/page.tsx && grep -q "WorkflowCards" app/page.tsx && grep -q "LandingCTA" app/page.tsx && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx :
      - `grep -q "import.*GeistSans.*from 'geist/font/sans'" app/layout.tsx` exit 0
      - `grep -q "import './globals.css'" app/layout.tsx` exit 0
      - `grep -q "lang=\"en\"" app/layout.tsx` exit 0
      - `grep -q "title: 'Voodoo Creative Radar'" app/layout.tsx` exit 0
      - `grep -q "description: 'From Market Signals to Testable Creatives'" app/layout.tsx` exit 0
      - PAS de `'use client'` : `grep -L "'use client'" app/layout.tsx | wc -l` retourne 1
    - components/landing/Hero.tsx :
      - Contient EXACTEMENT le titre `Voodoo Creative Radar` (verbatim PRD)
      - Contient EXACTEMENT la tagline `From Market Signals to Testable Creatives`
      - Contient EXACTEMENT le paragraphe `Analyse market ads, extract winning creative patterns, and generate a testable 30-second ad for a selected Voodoo game.`
    - components/landing/LandingCTA.tsx :
      - `grep -q "href=\"/project\"" components/landing/LandingCTA.tsx` exit 0
      - `grep -q ">Get Started<" components/landing/LandingCTA.tsx` exit 0
      - `grep -q "bg-\\[--color-accent\\]" components/landing/LandingCTA.tsx` exit 0
    - components/landing/WorkflowCards.tsx :
      - Les 4 titres PRD verbatim : `Select a game`, `Scan the market`, `Analyze winning patterns`, `Generate a creative` — `grep -c -E "Select a game|Scan the market|Analyze winning patterns|Generate a creative" components/landing/WorkflowCards.tsx` retourne au moins 4
      - Numéros `01`, `02`, `03`, `04` présents : `grep -cE "'(01|02|03|04)'" components/landing/WorkflowCards.tsx` retourne 4
      - `grep -q "text-\\[--color-accent\\]" components/landing/WorkflowCards.tsx` exit 0 (numéros magenta D-12)
    - app/page.tsx :
      - Importe et rend les 3 composants landing : `grep -q "Hero" app/page.tsx && grep -q "LandingCTA" app/page.tsx && grep -q "WorkflowCards" app/page.tsx`
      - Ordre D-13 : CTA AU-DESSUS du stepper. Vérifier que dans le JSX, `<LandingCTA />` apparaît AVANT `<WorkflowCards />` : `awk '/LandingCTA/{found=1} /WorkflowCards/{if(found)print "ok"}' app/page.tsx | grep -q ok` exit 0
      - PAS de `'use client'` : `grep -L "'use client'" app/page.tsx | wc -l` retourne 1
    - `pnpm exec tsc --noEmit` exit 0
    - `pnpm build` exit 0
    - Smoke test : `pnpm dev` démarre, requête HTTP `curl -s http://localhost:3000` retourne du HTML contenant "Voodoo Creative Radar" et "Get Started" (test manuel ou via background dev server)
  </acceptance_criteria>
  <done>app/layout.tsx configuré avec Geist Sans + Geist Mono via next/font/google, metadata title/description, lang="en", import globals.css ; 3 composants landing (Hero, WorkflowCards stepper horizontal, LandingCTA) avec contenu PRD strict ; app/page.tsx compose les 3 dans l'ordre D-13 (Hero → CTA → Stepper) ; pnpm build passe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Shell /project — AppShell + TopBar + Sidebar + WorkflowTabs + app/project/page.tsx</name>
  <read_first>
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (sections AppShell, Sidebar, WorkflowTabs)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-16 contenu sidebar, D-17 frontière Phase 1/2, D-18 'use client' rules)
    - components/ui/Tabs.tsx (Plan 03 — API consommée ici)
    - voodoo-creative-radar-prd.md (lignes 57-77 : Project page)
  </read_first>
  <files>
    - components/layout/AppShell.tsx
    - components/layout/TopBar.tsx
    - components/layout/Sidebar.tsx
    - components/layout/WorkflowTabs.tsx
    - app/project/page.tsx
  </files>
  <action>
**1. `components/layout/AppShell.tsx`** — Server component (D-18). Container plein écran avec slot enfant. Layout grid : sidebar (fixed width) + main content :

```typescript
import type { ReactNode } from 'react';

import { cn } from '@/lib/utils';

export interface AppShellProps {
  sidebar: ReactNode;
  topbar: ReactNode;
  children: ReactNode;
  className?: string;
}

export function AppShell({ sidebar, topbar, children, className }: AppShellProps) {
  return (
    <div className={cn('flex min-h-screen bg-[--color-bg]', className)}>
      <aside className="w-64 shrink-0 border-r border-[--color-border] bg-[--color-surface]">
        {sidebar}
      </aside>
      <div className="flex min-w-0 flex-1 flex-col">
        <header className="h-14 shrink-0 border-b border-[--color-border] bg-[--color-surface]">
          {topbar}
        </header>
        <main className="min-w-0 flex-1 overflow-auto">{children}</main>
      </div>
    </div>
  );
}
```

**2. `components/layout/TopBar.tsx`** — Server component, header projet (PRD ligne 59) :

```typescript
export function TopBar() {
  return (
    <div className="flex h-full items-center px-6">
      <h1 className="text-sm font-semibold text-[--color-charcoal]">
        Demo Project — Voodoo Creative Radar
      </h1>
    </div>
  );
}
```

**3. `components/layout/Sidebar.tsx`** — Server component (D-18). Contenu D-16 strict : titre, group `Projects`, `• Demo Project` actif, `+ New Project` grisé (curseur not-allowed, opacité 40%, aucun handler) :

```typescript
import { cn } from '@/lib/utils';

export function Sidebar() {
  return (
    <nav className="flex h-full flex-col gap-6 p-6" aria-label="Project navigation">
      <div>
        <span className="text-sm font-semibold text-[--color-charcoal]">
          Voodoo Creative Radar
        </span>
      </div>

      <div className="flex flex-col gap-2">
        <span className="text-xs font-semibold uppercase tracking-wide text-[--color-muted]">
          Projects
        </span>

        <ul className="flex flex-col gap-1">
          <li>
            <span
              aria-current="page"
              className="flex items-center gap-2 rounded-md bg-[--color-bg] px-3 py-1.5 text-sm font-medium text-[--color-charcoal]"
            >
              <span className="text-[--color-accent]" aria-hidden="true">•</span>
              Demo Project
            </span>
          </li>
        </ul>
      </div>

      <button
        type="button"
        disabled
        aria-disabled="true"
        title="Multi-project support is a future enhancement"
        className={cn(
          'mt-auto inline-flex items-center justify-start gap-2 rounded-md px-3 py-1.5 text-sm font-medium',
          'text-[--color-charcoal] opacity-40 cursor-not-allowed',
        )}
      >
        + New Project
      </button>
    </nav>
  );
}
```

Notes (D-16, "specifics" CONTEXT.md) :
- `+ New Project` est `disabled` HTML + `cursor-not-allowed` + `opacity-40` (charcoal 40%) + `aria-disabled="true"` + tooltip explicatif
- Le bullet `•` devant Demo Project est en magenta (single accent OK car indicateur d'état actif)
- Aucun handler onClick — bouton purement visuel

**4. `components/layout/WorkflowTabs.tsx`** — Client component obligatoire (D-18). State local en Phase 1 ; Phase 2 le branchera sur `useApp((s) => s.currentStep)` :

```typescript
'use client';

import { useState } from 'react';

import { Tabs, type Tab } from '@/components/ui/Tabs';

const TABS: readonly Tab[] = [
  { id: 'game', label: '1. Game Identity' },
  { id: 'market', label: '2. Market Scan' },
  { id: 'patterns', label: '3. Pattern Analysis' },
  { id: 'creative', label: '4. Creative Output' },
] as const;

const TAB_LABELS: Record<string, string> = {
  game: '1. Game Identity',
  market: '2. Market Scan',
  patterns: '3. Pattern Analysis',
  creative: '4. Creative Output',
};

export function WorkflowTabs() {
  // Phase 1: local state. Phase 2 will replace with:
  //   const activeTab = useApp((s) => s.currentStep);
  //   const setActiveTab = useApp((s) => s.setStep);
  const [activeTab, setActiveTab] = useState<string>('game');

  return (
    <div className="flex flex-col">
      <Tabs tabs={TABS} activeTab={activeTab} onTabChange={setActiveTab} />
      <div className="p-8">
        <p className="text-sm text-[--color-muted]">
          {`${TAB_LABELS[activeTab] ?? 'Étape'} — à venir`}
        </p>
      </div>
    </div>
  );
}
```

NB sur le placeholder par tab (D-17) : Phase 1 livre un placeholder texte simple `"<Tab Label> — à venir"`. Phase 2 remplace ça par un `<EmptyState>` ergonomique et branche `useApp` au store. La structure (state + onChange) reste la même → Phase 2 = pure substitution interne, l'API du composant ne change pas.

NB: l'export `Tab` depuis `@/components/ui/Tabs` est nécessaire — vérifier que Plan 03 a bien exporté l'interface (`export interface Tab`). Si l'executor découvre que Plan 03 n'a pas exporté `Tab`, ajouter un commit-fix dans `components/ui/Tabs.tsx` pour ajouter `export` devant `interface Tab`.

**5. `app/project/page.tsx`** — Server component qui compose le shell (D-16) :

```typescript
import { AppShell } from '@/components/layout/AppShell';
import { Sidebar } from '@/components/layout/Sidebar';
import { TopBar } from '@/components/layout/TopBar';
import { WorkflowTabs } from '@/components/layout/WorkflowTabs';

export default function ProjectPage() {
  return (
    <AppShell sidebar={<Sidebar />} topbar={<TopBar />}>
      <WorkflowTabs />
    </AppShell>
  );
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -L "'use client'" components/layout/AppShell.tsx components/layout/Sidebar.tsx components/layout/TopBar.tsx | wc -l | grep -q "^3$" && head -1 components/layout/WorkflowTabs.tsx | grep -q "'use client'" && grep -q "Demo Project — Voodoo Creative Radar" components/layout/TopBar.tsx && grep -q "Voodoo Creative Radar" components/layout/Sidebar.tsx && grep -q "Demo Project" components/layout/Sidebar.tsx && grep -q "+ New Project" components/layout/Sidebar.tsx && grep -q "cursor-not-allowed" components/layout/Sidebar.tsx && grep -q "opacity-40" components/layout/Sidebar.tsx && grep -q "1. Game Identity" components/layout/WorkflowTabs.tsx && grep -q "2. Market Scan" components/layout/WorkflowTabs.tsx && grep -q "3. Pattern Analysis" components/layout/WorkflowTabs.tsx && grep -q "4. Creative Output" components/layout/WorkflowTabs.tsx && grep -q "AppShell" app/project/page.tsx && grep -q "Sidebar" app/project/page.tsx && grep -q "TopBar" app/project/page.tsx && grep -q "WorkflowTabs" app/project/page.tsx && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - 4 composants layout + 1 page créés
    - AppShell.tsx, Sidebar.tsx, TopBar.tsx N'ONT PAS `'use client'` : `grep -L "'use client'" components/layout/{AppShell,Sidebar,TopBar}.tsx | wc -l` retourne 3
    - WorkflowTabs.tsx ligne 1 = `'use client';` (D-18)
    - TopBar.tsx contient EXACTEMENT le texte `Demo Project — Voodoo Creative Radar` (PRD ligne 59 — em-dash `—`)
    - Sidebar.tsx :
      - Contient `Voodoo Creative Radar` (titre)
      - Contient `Projects` (group label)
      - Contient `Demo Project` (item actif)
      - Contient `+ New Project` (item disabled)
      - `grep -q "cursor-not-allowed" components/layout/Sidebar.tsx` exit 0
      - `grep -q "opacity-40" components/layout/Sidebar.tsx` exit 0 (charcoal 40% per "specifics")
      - `grep -q "disabled" components/layout/Sidebar.tsx` exit 0 (button HTML disabled)
      - `grep -q "aria-current=\"page\"" components/layout/Sidebar.tsx` exit 0 (Demo Project marqué actif)
      - Le bouton `+ New Project` n'a AUCUN handler onClick : `grep -q "onClick" components/layout/Sidebar.tsx` retourne vide
    - WorkflowTabs.tsx :
      - 4 onglets exact verbatim : `grep -c -E "1\\. Game Identity|2\\. Market Scan|3\\. Pattern Analysis|4\\. Creative Output" components/layout/WorkflowTabs.tsx` retourne au moins 4
      - Les ids sont `game`, `market`, `patterns`, `creative` (matchent `AppState['currentStep']`) : `grep -c -E "id: '(game|market|patterns|creative)'" components/layout/WorkflowTabs.tsx` retourne 4
      - Utilise `useState` local pour activeTab : `grep -q "useState" components/layout/WorkflowTabs.tsx` exit 0
      - Importe `Tabs` depuis `@/components/ui/Tabs` : `grep -q "from '@/components/ui/Tabs'" components/layout/WorkflowTabs.tsx` exit 0
      - Affiche un placeholder texte par tab : `grep -q "à venir" components/layout/WorkflowTabs.tsx` exit 0
      - Aucun appel à `useApp` (D-17 : Phase 2 branchera) : `grep -q "useApp" components/layout/WorkflowTabs.tsx` retourne vide
    - app/project/page.tsx :
      - Importe les 4 composants layout depuis `@/components/layout/`
      - PAS de `'use client'`
    - `pnpm exec tsc --noEmit` exit 0
    - `pnpm build` exit 0
    - Smoke test runtime : `pnpm dev` puis `curl -s http://localhost:3000/project | grep -q "Demo Project — Voodoo Creative Radar"` exit 0
    - Pas de hydration warning : `pnpm dev` ne logge pas "Hydration failed" dans la console (PITFALLS.md — sera revérifié en Phase 2 quand le rehydrate Zustand sera ajouté)
  </acceptance_criteria>
  <done>Shell `/project` complet : AppShell server component avec slots sidebar + topbar + main, TopBar avec header verbatim PRD, Sidebar avec Voodoo Creative Radar + Projects + Demo Project actif (•) + New Project disabled (curseur not-allowed + opacity-40 + aria-disabled), WorkflowTabs client component avec 4 onglets PRD navigables via useState local + placeholder par tab "Étape — à venir" ; app/project/page.tsx compose le tout ; pnpm build passe ; routing landing → /project fonctionne.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL utilisateur (`/`, `/project`) → Server rendering | Surfaces publiques rendues côté serveur |
| Click `Get Started` → Navigation Next.js Link | Navigation client-side via Next router |
| Bouton `+ New Project` → No-op (D-16) | Risque de simulation d'action si l'utilisateur s'attend à un comportement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Bouton `+ New Project` qui semble cliquable mais ne fait rien | mitigate | Bouton HTML `disabled` + `cursor-not-allowed` + `opacity-40` + `aria-disabled="true"` + tooltip explicite "Multi-project support is a future enhancement" (Sidebar Task 2). L'utilisateur reçoit feedback visuel immédiat que c'est désactivé. |
| T-04-02 | Information Disclosure | metadata SEO (title, description) sur surface publique `/` | accept | Le titre + tagline sont déjà publics (PRD demande de les afficher). Aucun secret. |
| T-04-03 | Tampering | Hydration mismatch Next.js + Zustand persist côté client | mitigate | Phase 1 N'utilise PAS encore le store dans les composants (D-17). WorkflowTabs utilise `useState` local. Pas de localStorage read au mount → pas de mismatch SSR/client. Phase 2 branchera Zustand avec `useApp.persist.rehydrate()` dans un `useEffect` au layout (PITFALLS.md). |
| T-04-04 | DoS | Animation infinite ou loop dans le stepper | accept | WorkflowCards est statique (server component, pas d'animation). Pas de risque. |
| T-04-05 | Tampering | Geist font CDN compromis | accept | Geist est servi via `next/font/google` (Vercel) — empreinte vérifiée par Next.js, pas de risque pratique. |
| T-04-06 | Information Disclosure | URL `/project` accessible sans auth | accept | PRD : "outil interne mono-utilisateur démo" — pas d'auth en MVP (REQ-AUTH-01 est v2). Risque acceptable hors scope. |
</threat_model>

<verification>
1. `pnpm exec tsc --noEmit` exit 0
2. `pnpm build` exit 0
3. `pnpm dev` démarre sans erreur, sans warning hydration
4. `curl -s http://localhost:3000` retourne du HTML contenant `Voodoo Creative Radar`, `From Market Signals to Testable Creatives`, `Get Started`, et les 4 titres de cartes workflow
5. `curl -s http://localhost:3000/project` retourne du HTML contenant `Demo Project — Voodoo Creative Radar`, `Voodoo Creative Radar` (sidebar), `Demo Project`, `+ New Project`, et les 4 onglets `1. Game Identity`, `2. Market Scan`, `3. Pattern Analysis`, `4. Creative Output`
6. Cliquer `Get Started` sur `/` route bien vers `/project` (test manuel)
7. Cliquer un onglet sur `/project` change le placeholder affiché (test manuel — `useState` local fonctionne)
8. Aucun JSON brut visible nulle part (audit grep : `grep -r "JSON.stringify" app/ components/` retourne vide)
9. Single accent magenta limité aux endroits définis : CTA Get Started, numéros 01-04 stepper, bullet • Demo Project, focus rings, active tab indicator
</verification>

<success_criteria>
- [ ] `/` affiche titre + tagline + paragraphe + CTA + 4 cartes workflow numérotées (verbatim PRD)
- [ ] `Get Started` route vers `/project`
- [ ] `/project` affiche header `Demo Project — Voodoo Creative Radar`
- [ ] Sidebar affiche `Voodoo Creative Radar` + `Projects` + `• Demo Project` actif + `+ New Project` désactivé
- [ ] 4 onglets `1. Game Identity` / `2. Market Scan` / `3. Pattern Analysis` / `4. Creative Output` navigables
- [ ] AppShell + Sidebar + TopBar = server components ; WorkflowTabs = client component (D-18)
- [ ] Geist Sans + Geist Mono via next/font ; metadata title/description correct
- [ ] UI premium light : fond off-white, charcoal text, single accent magenta `#E91E63` strictement limité aux endroits définis
- [ ] No dark mode, no JSON brut visible
- [ ] `pnpm build` exit 0 ; `pnpm dev` sans warning hydration
- [ ] tsc --noEmit exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-landing/01-04-SUMMARY.md` documenting:
- Confirmation des contenus PRD verbatim sur landing + project page
- API des composants layout (AppShell props : sidebar/topbar/children, etc.) pour Phase 2
- Décision d'usage Geist via `.variable` + `font-sans` + `@theme` tokens font
- Pattern WorkflowTabs : useState local en Phase 1, à brancher sur `useApp((s) => s.currentStep)` en Phase 2
- Pattern Sidebar : `+ New Project` disabled + tooltip + opacity-40 (référence pour quand v2 sera implémenté)
- Confirmation absence de hydration warning (Phase 1 n'utilise pas le store dans le rendu)
</output>
