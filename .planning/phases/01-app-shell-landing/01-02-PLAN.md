---
phase: 01-app-shell-landing
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/types.ts
  - lib/copy.ts
  - lib/utils.ts
  - lib/state.ts
  - lib/sensorTower.ts
  - lib/gemini.ts
  - lib/scenario.ts
  - lib/prompts.ts
  - lib/scoring.ts
  - lib/formatters.ts
  - data/games.ts
autonomous: true
requirements:
  - REQ-file-architecture
user_setup: []
must_haves:
  truths:
    - "lib/types.ts exporte tous les types PRD verbatim (GameIdentity, MarketScanConfig, MarketAd, MarketScanResult, GeminiAdAnalysis, CreativePattern, PatternAnalysisResult, CreativeBrief, ScenarioPrompt, CreativeOutput, AppState)"
    - "lib/copy.ts exporte les 4 constantes loading texts verbatim PRD"
    - "lib/state.ts expose un store Zustand 5 typé `AppState & Actions` avec persist + skipHydration: true"
    - "lib/{sensorTower,gemini,scenario,prompts}.ts ont `import 'server-only'` dès la première ligne, même comme stubs"
    - "data/games.ts exporte exactement 2 jeux conformes au PRD (lignes 86-99)"
    - "tsc --noEmit passe sur tous les fichiers lib/ et data/"
  artifacts:
    - path: lib/types.ts
      provides: "Tous les types PRD type-only"
      contains: "type AppState"
    - path: lib/copy.ts
      provides: "4 constantes loading texts verbatim"
      contains: "Running Sensor Tower scan..."
    - path: lib/state.ts
      provides: "Hook useApp() Zustand 5 + persist + skipHydration"
      contains: "skipHydration: true"
    - path: lib/utils.ts
      provides: "Helper cn() basé sur clsx + tailwind-merge"
      contains: "twMerge"
    - path: data/games.ts
      provides: "2 jeux Voodoo hardcodés conformes PRD"
      contains: "Puzzle Voodoo Game"
    - path: lib/sensorTower.ts
      provides: "Stub server-only Sensor Tower (impl Phase 4)"
      contains: "import 'server-only'"
    - path: lib/gemini.ts
      provides: "Stub server-only Gemini (impl Phase 6)"
      contains: "import 'server-only'"
    - path: lib/scenario.ts
      provides: "Stub server-only Scenario (impl Phase 9)"
      contains: "import 'server-only'"
    - path: lib/prompts.ts
      provides: "Stub server-only loader prompts markdown (impl Phase 6)"
      contains: "import 'server-only'"
  key_links:
    - from: "lib/state.ts"
      to: "lib/types.ts"
      via: "import type { AppState } from '@/lib/types'"
      pattern: "from '@/lib/types'"
    - from: "data/games.ts"
      to: "lib/types.ts"
      via: "import type { GameIdentity } from '@/lib/types'"
      pattern: "GameIdentity.*from.*@/lib/types"
---

<objective>
Poser **toutes les fondations TypeScript du projet** : types PRD, constantes copy verbatim, store Zustand 5 hydration-safe, helper `cn()`, données jeux hardcodées, et stubs server-only des libs externes (Sensor Tower / Gemini / Scenario / prompts loader). Aucune logique métier n'est implémentée — les stubs préparent la barrière server-only **dès la compilation** pour bloquer toute fuite de secret côté client.

Purpose: Toutes les phases 2-9 dépendent de ces fondations. Le store Zustand `lib/state.ts` doit être correct dès Phase 1 (D-17 : Phase 2 le branche aux tabs, mais l'API est figée maintenant). Les stubs `import 'server-only'` posent la garde-fou compilation pour les Phases 4/6/9.
Output: 11 fichiers TypeScript posés, tous typés strict, `tsc --noEmit` passe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-landing/01-CONTEXT.md
@.planning/phases/01-app-shell-landing/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@voodoo-creative-radar-prd.md
@.planning/phases/01-app-shell-landing/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lib/types.ts (tous types PRD verbatim) + lib/copy.ts + data/games.ts + lib/utils.ts</name>
  <read_first>
    - voodoo-creative-radar-prd.md (lignes 84-100 : games ; lignes 108-114 : GameIdentity ; lignes 132-194 : MarketScanConfig + MarketAd + MarketScanResult ; lignes 217-234 : GeminiAdAnalysis ; lignes 270-293 : CreativePattern + PatternAnalysisResult ; lignes 348-396 : CreativeBrief + ScenarioPrompt + CreativeOutput + AppState)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-22 copy verbatim ; D-24 games)
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (sections lib/types.ts, lib/copy.ts, data/games.ts)
  </read_first>
  <files>
    - lib/types.ts
    - lib/copy.ts
    - lib/utils.ts
    - data/games.ts
  </files>
  <action>
1. **`lib/types.ts`** — fichier type-only (aucun runtime, pas de `'use client'` ni `'server-only'`). Exporter tous les types PRD verbatim (copie ligne par ligne du PRD, sans modification de noms ni de types) :

```typescript
// lib/types.ts
// Type definitions verbatim from voodoo-creative-radar-prd.md.
// No runtime — pure types, importable from server and client.

export type GameIdentity = {
  gameId: string;
  gameName: string;
  category: string;
  tags: string[];
};

export type MarketScanConfig = {
  category: string;
  tags: string[];
  numberOfAds: 10 | 20 | 30 | 50;
  timeRange: '30d' | '60d' | '90d';
  market: 'US' | 'France' | 'UK' | 'Global';
};

export type MarketAd = {
  id: string;
  rank?: number;
  gameName: string;
  adId: string;
  creativeUrl?: string;
  videoUrl?: string;
  imageUrl?: string;
  thumbnailUrl?: string;
  network?: string;
  format?: 'video' | 'image' | 'playable' | 'unknown';
  firstSeen?: string;
  lastSeen?: string;
  market?: string;
  performanceSignal?: number;
  performanceSignalLabel?: string;
  rankingReason?: string;
};

export type MarketScanResult = {
  config: MarketScanConfig;
  ads: MarketAd[];
  topAds: MarketAd[];
  selectedAd?: MarketAd;
};

export type GeminiAdAnalysis = {
  adId: string;
  videoSummary: string;
  openingHook: string;
  hook0To3s: string;
  sceneFlow: { timestamp: string; description: string }[];
  visualPatterns: string[];
  gameplayMechanics: string[];
  emotionalTriggers: string[];
  textOverlays: string[];
  cta: string;
  visualStyle: string;
  pacing: string;
  whyItWorks: string;
  applicabilityToGame: string;
  confidence: number;
};

export type CreativePattern = {
  id: string;
  rank: number;
  name: string;
  score: number;
  frequencyScore: number;
  gameFitScore: number;
  freshnessScore: number;
  creativeActionabilityScore: number;
  explanation: string;
  evidence: string;
  adaptationToGame: string;
  confidence: number;
};

export type PatternAnalysisResult = {
  selectedAd: MarketAd;
  geminiAnalysis: GeminiAdAnalysis;
  topPatterns: CreativePattern[];
  selectedPattern?: CreativePattern;
};

export type CreativeBrief = {
  concept: string;
  selectedPattern: string;
  openingHook: string;
  sceneFlow30s: { timestamp: string; description: string }[];
  visualDirection: string;
  gameplayReference: string;
  textOverlays: string[];
  cta: string;
  rationale: string;
  sourceEvidence: string;
  adaptationToGame: string;
};

export type ScenarioPrompt = {
  prompt: string;
  durationSec: 30;
  format: 'vertical_mobile_ad';
};

export type CreativeOutput = {
  selectedPattern: CreativePattern;
  brief: CreativeBrief;
  scenarioPrompt: ScenarioPrompt;
  scenarioVideoUrl?: string;
  status: 'idle' | 'generating' | 'complete' | 'error';
};

export type AppState = {
  currentStep: 'game' | 'market' | 'patterns' | 'creative';
  gameIdentity?: GameIdentity;
  marketScanConfig?: MarketScanConfig;
  marketScanResult?: MarketScanResult;
  selectedAd?: MarketAd;
  geminiAnalysis?: GeminiAdAnalysis;
  topPatterns?: CreativePattern[];
  selectedPattern?: CreativePattern;
  creativeBrief?: CreativeBrief;
  scenarioPrompt?: ScenarioPrompt;
  creativeOutput?: CreativeOutput;
};
```

2. **`lib/copy.ts`** — 4 constantes verbatim PRD (D-22, PRD lignes 502-506). JAMAIS paraphrasées :

```typescript
// lib/copy.ts
// Verbatim loading texts from the PRD. NEVER paraphrase.

export const RUNNING_SENSOR_TOWER_SCAN = 'Running Sensor Tower scan...';
export const ANALYZING_AD_WITH_GEMINI = 'Analyzing selected ad with Gemini...';
export const GENERATING_CREATIVE_BRIEF = 'Generating creative brief...';
export const GENERATING_30S_AD = 'Generating 30-second ad with Scenario...';
```

3. **`data/games.ts`** — 2 jeux PRD lignes 84-100, conformes au type `GameIdentity` (renommer `id`/`name` du PRD en `gameId`/`gameName` du type) :

```typescript
// data/games.ts
// 2 hardcoded games (PRD lines 86-99 — keep verbatim names, category, tags).
// MVP scope (D-24) — extension to >2 games is v2 (CAT-01).

import type { GameIdentity } from '@/lib/types';

export const GAMES: GameIdentity[] = [
  {
    gameId: 'puzzle-game',
    gameName: 'Puzzle Voodoo Game',
    category: 'Puzzle',
    tags: ['casual', 'logic', 'level-based', 'satisfying', 'challenge'],
  },
  {
    gameId: 'battle-game',
    gameName: 'Battle Voodoo Game',
    category: 'Battle',
    tags: ['combat', 'strategy', 'characters', 'progression', 'mid-core'],
  },
];
```

4. **`lib/utils.ts`** — helper `cn()` réutilisé par tous les UI atoms (Plan 03) :

```typescript
// lib/utils.ts
// className helper combining clsx + tailwind-merge.
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q "type AppState" lib/types.ts && grep -q "type GameIdentity" lib/types.ts && grep -q "type GeminiAdAnalysis" lib/types.ts && grep -q "type CreativeOutput" lib/types.ts && grep -q "Running Sensor Tower scan..." lib/copy.ts && grep -q "Analyzing selected ad with Gemini..." lib/copy.ts && grep -q "Generating creative brief..." lib/copy.ts && grep -q "Generating 30-second ad with Scenario..." lib/copy.ts && grep -q "Puzzle Voodoo Game" data/games.ts && grep -q "Battle Voodoo Game" data/games.ts && grep -q "twMerge" lib/utils.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export type" lib/types.ts` retourne 11 (les 11 types PRD : GameIdentity, MarketScanConfig, MarketAd, MarketScanResult, GeminiAdAnalysis, CreativePattern, PatternAnalysisResult, CreativeBrief, ScenarioPrompt, CreativeOutput, AppState)
    - `grep -q "currentStep: 'game' | 'market' | 'patterns' | 'creative'" lib/types.ts` exit 0
    - `grep -q "durationSec: 30" lib/types.ts` exit 0 (literal type, pas number)
    - `grep -q "format: 'vertical_mobile_ad'" lib/types.ts` exit 0 (literal type)
    - `lib/copy.ts` contient EXACTEMENT 4 exports : `RUNNING_SENSOR_TOWER_SCAN`, `ANALYZING_AD_WITH_GEMINI`, `GENERATING_CREATIVE_BRIEF`, `GENERATING_30S_AD`
    - Les 4 strings dans copy.ts sont verbatim PRD : `grep -q '"Running Sensor Tower scan\\.\\.\\."' lib/copy.ts` (ou simple quote), idem pour les 3 autres
    - `grep -c "gameId:" data/games.ts` retourne 2 (2 jeux)
    - `grep -q "casual.*logic.*level-based.*satisfying.*challenge" data/games.ts` exit 0 (tags Puzzle exact)
    - `grep -q "combat.*strategy.*characters.*progression.*mid-core" data/games.ts` exit 0 (tags Battle exact)
    - `grep -q "from '@/lib/types'" data/games.ts` exit 0
    - `grep -q "twMerge(clsx(inputs))" lib/utils.ts` exit 0
    - `pnpm exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>lib/types.ts contient les 11 types PRD verbatim ; lib/copy.ts contient les 4 constantes loading texts verbatim ; data/games.ts contient les 2 jeux PRD typés `GameIdentity[]` ; lib/utils.ts expose `cn()` ; `tsc --noEmit` passe sans erreur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: lib/state.ts — store Zustand 5 hydration-safe (persist + partialize + skipHydration)</name>
  <read_first>
    - lib/types.ts (créé en Task 1 — utilise `AppState`)
    - .planning/research/ARCHITECTURE.md (lignes 248-319 : Pattern 3 Zustand store)
    - .planning/research/PITFALLS.md (hydration mismatch Next.js + Zustand persist)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-17 frontière Phase 1/2)
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (section lib/state.ts)
  </read_first>
  <files>
    - lib/state.ts
  </files>
  <action>
Créer `lib/state.ts` exactement selon le pattern ARCHITECTURE.md Pattern 3 (lignes 267-315), avec `skipHydration: true` (D-17 + PITFALLS.md hydration mismatch). Phase 1 expose l'API ; Phase 2 ajoutera `useApp.persist.rehydrate()` dans un `useEffect` au root layout et le flag `hydrated`.

```typescript
// lib/state.ts
'use client';

import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import type {
  AppState,
  CreativeBrief,
  CreativeOutput,
  CreativePattern,
  GameIdentity,
  GeminiAdAnalysis,
  MarketAd,
  MarketScanConfig,
  MarketScanResult,
  ScenarioPrompt,
} from '@/lib/types';

// Actions exposed by the store. Phase 1 wires the API; Phase 2 connects to tabs.
type Actions = {
  setStep: (s: AppState['currentStep']) => void;
  setGameIdentity: (g: GameIdentity | undefined) => void;
  setMarketScanConfig: (c: MarketScanConfig | undefined) => void;
  setMarketScanResult: (r: MarketScanResult | undefined) => void;
  setSelectedAd: (a: MarketAd | undefined) => void;
  setGeminiAnalysis: (a: GeminiAdAnalysis | undefined) => void;
  setTopPatterns: (p: CreativePattern[] | undefined) => void;
  setSelectedPattern: (p: CreativePattern | undefined) => void;
  setCreativeBrief: (b: CreativeBrief | undefined) => void;
  setScenarioPrompt: (p: ScenarioPrompt | undefined) => void;
  setCreativeOutput: (o: CreativeOutput | undefined) => void;
  reset: () => void;
};

const initial: AppState = { currentStep: 'game' };

export const useApp = create<AppState & Actions>()(
  persist(
    (set) => ({
      ...initial,
      setStep: (currentStep) => set({ currentStep }),
      setGameIdentity: (gameIdentity) => set({ gameIdentity }),
      setMarketScanConfig: (marketScanConfig) => set({ marketScanConfig }),
      setMarketScanResult: (marketScanResult) => set({ marketScanResult }),
      setSelectedAd: (selectedAd) => set({ selectedAd }),
      setGeminiAnalysis: (geminiAnalysis) => set({ geminiAnalysis }),
      setTopPatterns: (topPatterns) => set({ topPatterns }),
      setSelectedPattern: (selectedPattern) => set({ selectedPattern }),
      setCreativeBrief: (creativeBrief) => set({ creativeBrief }),
      setScenarioPrompt: (scenarioPrompt) => set({ scenarioPrompt }),
      setCreativeOutput: (creativeOutput) => set({ creativeOutput }),
      reset: () => set(initial),
    }),
    {
      name: 'vcr.appstate.v1',
      storage: createJSONStorage(() => localStorage),
      // Persist only domain state, never transient UI flags (loading/error).
      partialize: (s) => ({
        currentStep: s.currentStep,
        gameIdentity: s.gameIdentity,
        marketScanConfig: s.marketScanConfig,
        marketScanResult: s.marketScanResult,
        selectedAd: s.selectedAd,
        geminiAnalysis: s.geminiAnalysis,
        topPatterns: s.topPatterns,
        selectedPattern: s.selectedPattern,
        creativeBrief: s.creativeBrief,
        scenarioPrompt: s.scenarioPrompt,
        creativeOutput: s.creativeOutput,
      }),
      // CRITICAL (PITFALLS.md): skipHydration prevents Next.js App Router hydration mismatch.
      // Phase 2 calls useApp.persist.rehydrate() from a useEffect in app/layout.tsx
      // and gates UI on a `hydrated` flag.
      skipHydration: true,
    },
  ),
);
```

**Note D-17** : ce plan ne touche PAS à `app/layout.tsx` (le `useEffect` rehydrate est livré en Phase 2). Phase 1 expose l'API uniquement.
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q "'use client'" lib/state.ts && grep -q "skipHydration: true" lib/state.ts && grep -q "createJSONStorage" lib/state.ts && grep -q "partialize" lib/state.ts && grep -q "name: 'vcr.appstate.v1'" lib/state.ts && grep -q "from 'zustand'" lib/state.ts && grep -q "from '@/lib/types'" lib/state.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 lib/state.ts` (ou les 2 premières lignes) contient `'use client';`
    - `grep -q "skipHydration: true" lib/state.ts` exit 0 (D-17 + PITFALLS.md)
    - `grep -q "name: 'vcr.appstate.v1'" lib/state.ts` exit 0 (clé localStorage exacte)
    - `grep -q "createJSONStorage(() => localStorage)" lib/state.ts` exit 0
    - `grep -q "partialize:" lib/state.ts` exit 0 (seuls les champs domaine persistés)
    - Le store exporte `useApp` : `grep -q "export const useApp" lib/state.ts` exit 0
    - `partialize` n'inclut AUCUN champ transient `loading` ou `error` (vérifier : `grep -A 15 "partialize:" lib/state.ts | grep -E "loading|error"` retourne rien)
    - 11 actions setter sont définies (une par champ d'AppState sauf `currentStep` qui a `setStep`) : `grep -cE "^\\s+set[A-Z]" lib/state.ts` retourne au moins 11
    - `pnpm exec tsc --noEmit` exit 0 (typage strict respecté avec exactOptionalPropertyTypes — les setters acceptent `T | undefined`)
  </acceptance_criteria>
  <done>lib/state.ts crée le store Zustand 5 typé `AppState & Actions` avec persist (key `vcr.appstate.v1`, localStorage, partialize sans champs transient, skipHydration: true) ; toutes les actions setter typées ; `'use client'` directive en tête ; tsc --noEmit passe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Stubs server-only lib/{sensorTower,gemini,scenario,prompts,scoring,formatters}.ts</name>
  <read_first>
    - lib/types.ts (créé en Task 1)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-20 server-only enforcement)
    - .planning/research/ARCHITECTURE.md (server/client boundary)
    - voodoo-creative-radar-prd.md (signatures fonctions : lignes 144 sensorTower, 206-210 gemini, 240-246 scoring, 303-305 scenario)
  </read_first>
  <files>
    - lib/sensorTower.ts
    - lib/gemini.ts
    - lib/scenario.ts
    - lib/prompts.ts
    - lib/scoring.ts
    - lib/formatters.ts
  </files>
  <action>
Créer 6 stubs typés correspondant aux signatures finales des fonctions PRD. **Les 4 premiers (sensorTower, gemini, scenario, prompts) DOIVENT avoir `import 'server-only'` en première ligne (D-20)** — bloque la fuite de secrets côté client à la compilation. Les 2 derniers (scoring, formatters) sont purs et utilisables côté client.

Chaque stub doit :
- Compiler sans erreur (pas de `any`, pas d'unimplemented)
- Lever une `Error('Not implemented in Phase 1 — see Phase X')` si appelé en runtime
- Exporter la signature finale avec types corrects depuis `@/lib/types`

1. **`lib/sensorTower.ts`** (D-20 — implémenté Phase 4) :
```typescript
import 'server-only';

import type { MarketScanConfig, MarketScanResult } from '@/lib/types';

/**
 * Calls Sensor Tower live to fetch market ads matching config.
 * Implemented in Phase 4 (REQ-step2-market-scan).
 */
export async function fetchSensorTowerAds(
  _config: MarketScanConfig,
): Promise<MarketScanResult> {
  throw new Error('fetchSensorTowerAds not implemented in Phase 1 — see Phase 4');
}
```

2. **`lib/gemini.ts`** (D-20 — implémenté Phase 6) :
```typescript
import 'server-only';

import type { GameIdentity, GeminiAdAnalysis, MarketAd } from '@/lib/types';

/**
 * Analyzes the selected ad's full video with Gemini.
 * Implemented in Phase 6 (REQ-step3-pattern-analysis — Gemini side).
 */
export async function analyzeSelectedAdWithGemini(
  _ad: MarketAd,
  _game: GameIdentity,
): Promise<GeminiAdAnalysis> {
  throw new Error('analyzeSelectedAdWithGemini not implemented in Phase 1 — see Phase 6');
}
```

3. **`lib/scenario.ts`** (D-20 — `buildScenarioPrompt` Phase 8 ; `generateScenarioAd` Phase 9) :
```typescript
import 'server-only';

import type {
  CreativeBrief,
  CreativeOutput,
  CreativePattern,
  GameIdentity,
  ScenarioPrompt,
} from '@/lib/types';

/**
 * Builds the Scenario prompt from brief + game + selected pattern.
 * Pure function — Implemented in Phase 8 (REQ-step4-creative-output, brief + prompt).
 */
export function buildScenarioPrompt(
  _brief: CreativeBrief,
  _game: GameIdentity,
  _pattern: CreativePattern,
): ScenarioPrompt {
  throw new Error('buildScenarioPrompt not implemented in Phase 1 — see Phase 8');
}

/**
 * Generates the final 30s vertical ad via Scenario API.
 * Implemented in Phase 9 (REQ-step4-creative-output, video generation).
 */
export async function generateScenarioAd(_prompt: ScenarioPrompt): Promise<CreativeOutput> {
  throw new Error('generateScenarioAd not implemented in Phase 1 — see Phase 9');
}
```

4. **`lib/prompts.ts`** (D-20 — markdown loader, implémenté Phase 6) :
```typescript
import 'server-only';

/**
 * Loads a prompt template from prompts/<slug>.md.
 * Implemented in Phase 6 — used by gemini.ts, scenario.ts.
 */
export async function loadPrompt(_slug: string): Promise<string> {
  throw new Error('loadPrompt not implemented in Phase 1 — see Phase 6');
}
```

5. **`lib/scoring.ts`** (PAS de server-only — pure function, testable côté client en Phase 7) :
```typescript
import type {
  CreativePattern,
  GameIdentity,
  GeminiAdAnalysis,
  MarketAd,
} from '@/lib/types';

/**
 * Generates exactly 3 ranked creative patterns from a Gemini analysis + ad + game.
 * Pure scoring function (no I/O). Formula:
 *   Pattern Score = 35% Frequency + 25% Game Fit + 20% Freshness + 20% Creative Actionability
 * Implemented in Phase 7 (REQ-step3-pattern-analysis — scoring side).
 */
export function generateTopPatterns(
  _analysis: GeminiAdAnalysis,
  _ad: MarketAd,
  _game: GameIdentity,
): CreativePattern[] {
  throw new Error('generateTopPatterns not implemented in Phase 1 — see Phase 7');
}
```

6. **`lib/formatters.ts`** (PAS de server-only — utilitaires d'affichage, utilisés par les composants UI) :
```typescript
/**
 * Formatters for display values (dates, percentages, large numbers).
 * Stubbed in Phase 1 — concrete implementations added as needed in later phases.
 */

export function formatPercent(value: number, fractionDigits = 1): string {
  return `${(value * 100).toFixed(fractionDigits)}%`;
}

export function formatNumber(value: number): string {
  return new Intl.NumberFormat('en-US').format(value);
}

export function formatDate(iso: string): string {
  return new Date(iso).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && head -1 lib/sensorTower.ts | grep -q "import 'server-only'" && head -1 lib/gemini.ts | grep -q "import 'server-only'" && head -1 lib/scenario.ts | grep -q "import 'server-only'" && head -1 lib/prompts.ts | grep -q "import 'server-only'" && grep -q "fetchSensorTowerAds" lib/sensorTower.ts && grep -q "analyzeSelectedAdWithGemini" lib/gemini.ts && grep -q "buildScenarioPrompt" lib/scenario.ts && grep -q "generateScenarioAd" lib/scenario.ts && grep -q "loadPrompt" lib/prompts.ts && grep -q "generateTopPatterns" lib/scoring.ts && grep -q "formatPercent" lib/formatters.ts && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 lib/sensorTower.ts` est exactement `import 'server-only';`
    - `head -1 lib/gemini.ts` est exactement `import 'server-only';`
    - `head -1 lib/scenario.ts` est exactement `import 'server-only';`
    - `head -1 lib/prompts.ts` est exactement `import 'server-only';`
    - `head -1 lib/scoring.ts` ne contient PAS `'server-only'` (pur, utilisable côté client)
    - `head -1 lib/formatters.ts` ne contient PAS `'server-only'`
    - Signatures conformes au PRD :
      - `grep -q "fetchSensorTowerAds(_config: MarketScanConfig): Promise<MarketScanResult>" lib/sensorTower.ts` exit 0
      - `grep -q "analyzeSelectedAdWithGemini" lib/gemini.ts && grep -q "Promise<GeminiAdAnalysis>" lib/gemini.ts` exit 0
      - `grep -q "buildScenarioPrompt" lib/scenario.ts && grep -q ": ScenarioPrompt" lib/scenario.ts` exit 0
      - `grep -q "generateScenarioAd(_prompt: ScenarioPrompt): Promise<CreativeOutput>" lib/scenario.ts` exit 0
      - `grep -q "generateTopPatterns" lib/scoring.ts && grep -q "CreativePattern\\[\\]" lib/scoring.ts` exit 0
    - `pnpm exec tsc --noEmit` exit 0 (typage strict + exactOptionalPropertyTypes respecté)
    - `pnpm build` exit 0 (le bundler ne se plaint pas de l'import 'server-only' tant que ces libs ne sont pas importées par un client component — elles ne le sont pas en Phase 1)
  </acceptance_criteria>
  <done>6 stubs typés créés ; les 4 stubs server-only (sensorTower, gemini, scenario, prompts) ont `import 'server-only';` en première ligne ; les 2 purs (scoring, formatters) sont importables côté client ; toutes les signatures matchent le PRD ; chaque fonction non-implémentée throw une Error explicite mentionnant la phase d'implémentation ; pnpm build passe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code serveur (lib/{sensorTower,gemini,scenario,prompts}) → Bundle client | Risque de leak de secrets API si une route client importe accidentellement ces libs |
| localStorage (browser) → Code client (Zustand persist) | Données utilisateur (état du wizard) stockées en clair sur le navigateur |
| Module loader (require/import) → Bundler Next.js | Risque que `import 'server-only'` soit ignoré ou contourné |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | lib/{sensorTower,gemini,scenario,prompts}.ts | mitigate | `import 'server-only';` en première ligne (Task 3) — Next.js bundler lève une compile-error si une client lib importe ces modules ; vérification `head -1` automatisée |
| T-02-02 | Information Disclosure | lib/state.ts (localStorage persist) | accept | Le store contient l'état applicatif user (game, brief, prompts) sans secrets API. Tag `vcr.appstate.v1` permet de rincer le storage en cas de schema change. PRD autorise localStorage explicite. |
| T-02-03 | Tampering | localStorage modifié à la main par l'utilisateur | accept | Outil interne mono-utilisateur (PRD : no auth, no multi-user). Aucune action dommageable ne découle d'un state altéré ; au pire le user re-run le workflow. |
| T-02-04 | Tampering | Schema mismatch après évolution du type AppState | mitigate | Clé `vcr.appstate.v1` versionnée. Phase ultérieure qui modifie AppState bumpera à `v2` (rincer automatiquement les anciens états). |
| T-02-05 | Information Disclosure | Persistance d'objets MarketAd contenant `creativeUrl`/`videoUrl` (URLs CDN signées Sensor Tower) | accept | URLs déjà publiques (signed mais accessibles depuis le navigateur user). Aucun secret derrière. |
| T-02-06 | Spoofing | Stub `throw Error` pourrait être appelé accidentellement en production | mitigate | Tous les stubs lèvent une Error explicite mentionnant la phase d'implémentation. Aucun appel runtime en Phase 1 (les UI components Phase 1 sont statiques, pas d'event handler qui appellerait ces libs). |
| T-02-07 | Repudiation | Modifications du store non journalisées | accept | Pas de besoin d'audit log pour un outil interne mono-utilisateur. |
</threat_model>

<verification>
1. `pnpm exec tsc --noEmit` exit 0 (typage strict respecté avec les 3 flags)
2. `pnpm build` exit 0 (la build Next.js ne casse pas — les stubs server-only sont OK car aucun client component ne les importe encore)
3. Les 4 fichiers server-only ont `import 'server-only';` en ligne 1 — vérifié par grep automatisé
4. Le store Zustand a `skipHydration: true` (PITFALLS.md hydration safe)
5. Les 11 types PRD sont exportés depuis `lib/types.ts`
6. Les 4 strings copy verbatim sont présents dans `lib/copy.ts` (caractères exacts incluant les `...`)
7. data/games.ts contient exactement 2 jeux conformes au PRD
</verification>

<success_criteria>
- [ ] lib/types.ts exporte 11 types PRD verbatim
- [ ] lib/copy.ts exporte 4 constantes loading texts verbatim
- [ ] lib/utils.ts expose `cn()`
- [ ] data/games.ts exporte `GAMES: GameIdentity[]` avec exactement 2 jeux
- [ ] lib/state.ts est un store Zustand 5 client-only avec persist + partialize + skipHydration: true (key `vcr.appstate.v1`)
- [ ] lib/{sensorTower,gemini,scenario,prompts}.ts ont `import 'server-only';` en ligne 1
- [ ] lib/{scoring,formatters}.ts sont purs (pas server-only)
- [ ] Toutes les fonctions stub ont des signatures conformes au PRD et throw une Error explicite
- [ ] `pnpm exec tsc --noEmit` exit 0
- [ ] `pnpm build` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-landing/01-02-SUMMARY.md` documenting:
- Liste des 11 types PRD exportés depuis lib/types.ts (avec ligne du PRD source)
- Confirmation des 4 constantes copy verbatim
- Décisions Zustand : key localStorage, champs persistés vs transient, skipHydration: true
- Phases qui implémenteront chaque stub (Phase 4 = sensorTower, Phase 6 = gemini + prompts, Phase 7 = scoring, Phase 8 = buildScenarioPrompt, Phase 9 = generateScenarioAd)
- Confirmation server-only enforcement sur les 4 libs API
</output>
