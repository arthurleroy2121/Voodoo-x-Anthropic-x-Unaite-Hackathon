---
phase: 01-app-shell-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/project/page.tsx
  - .env.local.example
  - README.md
  - .planning/research/sensortower-reference.ts
autonomous: true
requirements:
  - REQ-file-architecture
  - REQ-env-variables
user_setup: []
must_haves:
  truths:
    - "`pnpm install` puis `pnpm dev` démarre Next.js 16.2.x sans erreur sur http://localhost:3000"
    - "`pnpm build` passe (pas de Vite résiduel, tsconfig strict respecté)"
    - "Le tag git `vite-sandbox-final` existe et pointe sur le dernier commit Vite"
    - "`.env.local.example` contient les 4 variables PRD ; `.env*` est git-ignoré"
    - "L'arborescence `app/`, `components/`, `lib/`, `prompts/`, `data/`, `public/` existe avec stubs minimaux"
  artifacts:
    - path: package.json
      provides: "Next.js 16.2.x + React 19.2 + TS 5.6+ + Tailwind 4.1 + Zustand 5 + clsx + tailwind-merge + zod + lucide-react + Geist font"
      contains: '"next": "^16.2'
    - path: tsconfig.json
      provides: "TypeScript strict avec noUncheckedIndexedAccess + exactOptionalPropertyTypes"
      contains: '"noUncheckedIndexedAccess": true'
    - path: app/globals.css
      provides: "Tailwind v4 zéro-config + @theme tokens design system"
      contains: '@import "tailwindcss"'
    - path: .env.local.example
      provides: "4 variables d'env PRD avec valeurs vides"
      contains: "SENSOR_TOWER_API_KEY="
    - path: .gitignore
      provides: ".next/, node_modules/, .env*.local, *.tsbuildinfo, .vercel/ ignorés"
      contains: ".env*.local"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "app/layout.tsx"
      to: "next/font/google ou geist"
      via: "Geist Sans className sur <html>"
      pattern: "GeistSans|Geist"
---

<objective>
Remplacer la sandbox Vite existante par un nouveau projet Next.js 16.2 (App Router + TS strict + Tailwind v4 zéro-config) à la racine de `voodoo-hack/`. Préserver l'historique Vite via tag git `vite-sandbox-final`. Poser l'arborescence PRD complète, le tsconfig strict, les tokens design system dans `app/globals.css`, et le `.env.local.example`. **Aucune feature applicative dans ce plan** — les fondations code (types, state, UI atoms) sont posées par les plans 02 et 03 en Wave 2.

Purpose: Sans ce scaffold propre, aucun autre plan ne peut s'exécuter (collision Vite/Next, mauvais tsconfig, tokens manquants). C'est l'unique plan bloquant de la phase.
Output: Un projet Next.js qui `pnpm dev` et `pnpm build` proprement, avec l'arborescence dossier complète et les conventions design lockées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-shell-landing/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@voodoo-creative-radar-prd.md
@CLAUDE.md

<interfaces>
<!-- Tokens design system locked par CONTEXT.md D-08, D-10. Tous les composants downstream consomment via var(--color-*) ou classes Tailwind utility -->

D-10 palette tokens (à exposer dans app/globals.css `@theme`):
```
--color-bg: #fafafa
--color-surface: #ffffff
--color-charcoal: #1a1a1a
--color-muted: #6b7280
--color-border: #e5e5e5
--color-accent: #E91E63
```

D-25 .env.local.example exact content:
```
SENSOR_TOWER_API_KEY=
GEMINI_API_KEY=
SCENARIO_API_KEY=
NEXT_PUBLIC_USE_DEV_MOCKS=false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag git de la sandbox Vite + cleanup Vite-specific files</name>
  <read_first>
    - voodoo-hack/.gitignore (état actuel)
    - voodoo-hack/package.json (état Vite actuel — sera supprimé après tag)
    - voodoo-hack/CLAUDE.md (sera mis à jour après scaffold)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-02 à D-07)
  </read_first>
  <files>
    - .gitignore (étendu)
    - (suppressions) src/, index.html, vite.config.ts, tsconfig.app.json, tsconfig.node.json, tsconfig.app.tsbuildinfo, tsconfig.node.tsbuildinfo, dist/, vercel.json, package.json (Vite), pnpm-lock.yaml (Vite)
    - (déplacement) voodoo-hack/api/sensortower.ts → .planning/research/sensortower-reference.ts
  </files>
  <action>
**ATTENTION : ordre strict, le tag DOIT être créé AVANT toute suppression (D-04).**

1. Vérifier qu'on est sur `main` avec `git status` propre — sinon stash/commit d'abord.

2. Créer le tag `vite-sandbox-final` sur HEAD (le dernier commit Vite avant remplacement) :
   ```bash
   git tag -a vite-sandbox-final -m "Final Vite sandbox state before Next.js Creative Radar scaffold"
   ```

3. Déplacer le fichier de référence Sensor Tower vers le dossier de recherche (D-03) :
   ```bash
   mkdir -p .planning/research
   git mv voodoo-hack/api/sensortower.ts .planning/research/sensortower-reference.ts
   ```
   Si le fichier est à `voodoo-hack/api/sensortower.ts` (cwd hackathon parent), adapter chemin. Si on est déjà dans `voodoo-hack/`, utiliser `git mv api/sensortower.ts .planning/research/sensortower-reference.ts`.

4. Supprimer (via `git rm -r` quand tracké, sinon `rm -rf`) les artefacts Vite suivants à la racine de voodoo-hack/ (D-05) :
   - `src/`
   - `index.html`
   - `vite.config.ts`
   - `tsconfig.app.json`, `tsconfig.node.json`, `tsconfig.app.tsbuildinfo`, `tsconfig.node.tsbuildinfo`
   - `tsconfig.json` (l'actuel Vite — sera regénéré par next-app)
   - `dist/`
   - `node_modules/`
   - `pnpm-lock.yaml`
   - `vercel.json`
   - `package.json`
   - `api/` (vide après déplacement de sensortower.ts)
   - `public/` (sera regénéré par next-app)

5. Étendre `.gitignore` à la racine de voodoo-hack/ avec les entrées Next.js (cumul, ne pas supprimer les entrées existantes utiles) :
   ```
   # Next.js
   .next/
   out/
   *.tsbuildinfo
   next-env.d.ts

   # Vercel
   .vercel/

   # Node
   node_modules/

   # Env
   .env
   .env.local
   .env*.local

   # Logs
   npm-debug.log*
   yarn-debug.log*
   yarn-error.log*
   pnpm-debug.log*

   # Editor
   .DS_Store
   ```
   Vérifier qu'aucun secret n'est tracké : `git ls-files | grep -E "\\.env" | grep -v example` doit ne renvoyer que `.env.local.example` (qui n'existe pas encore — donc rien).
  </action>
  <verify>
    <automated>git tag -l | grep -q "^vite-sandbox-final$" && test ! -f voodoo-hack/vite.config.ts && test ! -f voodoo-hack/index.html && test -f .planning/research/sensortower-reference.ts && grep -q "\\.env\\*\\.local" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l vite-sandbox-final` retourne `vite-sandbox-final` (exit 0)
    - `git show vite-sandbox-final --stat` affiche un commit Vite valide (présence de `vite.config.ts` ou `src/App.tsx` dans son arborescence)
    - `ls voodoo-hack/src/` retourne "No such file or directory"
    - `ls voodoo-hack/vite.config.ts` retourne "No such file or directory"
    - `ls voodoo-hack/index.html` retourne "No such file or directory"
    - `cat .planning/research/sensortower-reference.ts` affiche du code (le fichier existe et a été déplacé)
    - `grep -q "\\.next/" voodoo-hack/.gitignore` exit 0
    - `grep -q "\\.env\\*\\.local" voodoo-hack/.gitignore` exit 0
    - `grep -q "node_modules/" voodoo-hack/.gitignore` exit 0
  </acceptance_criteria>
  <done>Tag git vite-sandbox-final créé pointant sur la dernière révision Vite ; tous les fichiers Vite-specific listés ci-dessus supprimés ; api/sensortower.ts déplacé en .planning/research/sensortower-reference.ts ; .gitignore étendu avec les patterns Next.js + Vercel + env files.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 16.2 + dépendances + tsconfig strict + arborescence PRD</name>
  <read_first>
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-19, D-20, D-23)
    - .planning/research/STACK.md (versions exactes Next/Tailwind/Zustand)
    - voodoo-creative-radar-prd.md (section "FILE ARCHITECTURE", lignes 403-476)
  </read_first>
  <files>
    - package.json (créé par next-app)
    - pnpm-lock.yaml (créé par pnpm)
    - tsconfig.json (modifié pour ajouter noUncheckedIndexedAccess + exactOptionalPropertyTypes)
    - next.config.ts (créé par next-app, laissé tel quel ou minimal)
    - app/layout.tsx (généré, sera retravaillé en Plan 04)
    - app/page.tsx (généré, sera remplacé en Plan 04)
    - app/globals.css (généré, sera réécrit en Task 3 ci-dessous)
    - public/ (généré)
    - eslint.config.mjs (généré flat config)
    - .prettierrc (créé)
    - components/{layout,landing,game,market,patterns,creative,ui}/.gitkeep
    - lib/.gitkeep
    - prompts/.gitkeep
    - data/.gitkeep
    - app/project/.gitkeep
  </files>
  <action>
1. Depuis la racine de `voodoo-hack/`, scaffolder Next.js (D-19) :
   ```bash
   pnpm create next-app@latest . --typescript --tailwind --eslint --app --turbopack --src-dir=false --import-alias="@/*" --use-pnpm --yes
   ```
   - `.` cible le répertoire courant (voodoo-hack/)
   - Si pnpm refuse car le dossier n'est pas vide (présence de `.planning/`, `CLAUDE.md`, `voodoo-creative-radar-prd.md`), passer `--yes` qui accepte le scaffold dans un dossier non-vide. Sinon, exécuter `pnpm create next-app@latest temp-scaffold ...` puis copier le contenu généré (sans écraser `.planning/`, `CLAUDE.md`, `voodoo-creative-radar-prd.md`, `.gitignore` étendu, `.mcp.json` s'il existe).
   - Confirmer la version installée : `cat package.json | grep '"next"'` doit afficher `^16.2` ou `16.2.x`.

2. Installer les deps applicatives (D-19) :
   ```bash
   pnpm add zustand@^5.0.0 clsx tailwind-merge zod lucide-react geist
   pnpm add -D prettier prettier-plugin-tailwindcss
   ```

3. Modifier `tsconfig.json` pour ajouter les flags stricts (D-19) :
   - Ouvrir `tsconfig.json` généré par next-app
   - Dans `compilerOptions`, ajouter (s'ils ne sont pas présents) :
     ```json
     "strict": true,
     "noUncheckedIndexedAccess": true,
     "exactOptionalPropertyTypes": true
     ```
   - Vérifier que `"strict": true` est bien présent (devrait l'être par défaut depuis next-app).

4. Créer `.prettierrc` à la racine :
   ```json
   {
     "plugins": ["prettier-plugin-tailwindcss"],
     "semi": true,
     "singleQuote": true,
     "trailingComma": "all",
     "printWidth": 100
   }
   ```

5. Créer l'arborescence dossier PRD (lignes 405-475 du PRD), avec un `.gitkeep` dans chaque dossier vide :
   ```bash
   mkdir -p components/layout components/landing components/game components/market components/patterns components/creative components/ui
   mkdir -p lib prompts data app/project
   touch components/layout/.gitkeep components/landing/.gitkeep components/game/.gitkeep components/market/.gitkeep components/patterns/.gitkeep components/creative/.gitkeep components/ui/.gitkeep
   touch lib/.gitkeep prompts/.gitkeep data/.gitkeep
   ```
   Note : `app/project/page.tsx` sera créé par Task 4 du Plan 04 (pas un .gitkeep).

6. Lancer `pnpm install` pour s'assurer que tout est résolu, puis `pnpm dev` pour valider le scaffold (Ctrl+C après "Ready"). Puis `pnpm build` pour valider qu'il compile.
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q '"next": "\\^16\\.2' package.json && grep -q '"noUncheckedIndexedAccess": true' tsconfig.json && grep -q '"exactOptionalPropertyTypes": true' tsconfig.json && grep -q '"zustand"' package.json && grep -q '"zod"' package.json && grep -q '"lucide-react"' package.json && test -d components/ui && test -d components/layout && test -d lib && test -d prompts && test -d data && test -d app/project && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `cat package.json | grep '"next"'` affiche `"next": "^16.2.x"` ou `"^16.2"` (ou similaire 16.2.*)
    - `cat package.json | grep '"react"'` affiche `"react": "^19.2"` ou similaire 19.x
    - `cat package.json | grep '"zustand"'` affiche `"zustand": "^5.`
    - `cat package.json | grep '"zod"'` exit 0
    - `cat package.json | grep '"clsx"'` exit 0
    - `cat package.json | grep '"tailwind-merge"'` exit 0
    - `cat package.json | grep '"lucide-react"'` exit 0
    - `cat package.json | grep '"geist"'` exit 0
    - `cat package.json | grep '"prettier-plugin-tailwindcss"'` exit 0
    - `grep -q '"noUncheckedIndexedAccess": true' tsconfig.json` exit 0
    - `grep -q '"exactOptionalPropertyTypes": true' tsconfig.json` exit 0
    - `grep -q '"strict": true' tsconfig.json` exit 0
    - Tous les dossiers existent : `test -d components/{ui,layout,landing,game,market,patterns,creative} && test -d lib && test -d prompts && test -d data && test -d app/project`
    - `pnpm build` exit 0 (compilation réussie sans erreur TypeScript)
    - `cat .prettierrc | grep -q "prettier-plugin-tailwindcss"` exit 0
  </acceptance_criteria>
  <done>Next.js 16.2 scaffolé à la racine de voodoo-hack/, dépendances applicatives installées (zustand 5, clsx, tailwind-merge, zod, lucide-react, geist) et dev (prettier + plugin tailwindcss), tsconfig strict avec les 3 flags activés, arborescence PRD complète créée, `.prettierrc` créé, `pnpm build` réussit.</done>
</task>

<task type="auto">
  <name>Task 3: app/globals.css avec @theme tokens + .env.local.example + README minimal</name>
  <read_first>
    - app/globals.css (généré par next-app — sera réécrit)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-08, D-10, D-11, D-25)
    - .planning/research/STACK.md (Tailwind v4 zéro-config syntax)
    - voodoo-creative-radar-prd.md (lignes 480-490 ENV VARIABLES)
  </read_first>
  <files>
    - app/globals.css
    - .env.local.example
    - README.md
  </files>
  <action>
1. **Réécrire `app/globals.css`** (totalement, le contenu généré par next-app est remplacé) avec les tokens design system PRD (D-10) :

   ```css
   @import "tailwindcss";

   @theme {
     --color-bg: #fafafa;
     --color-surface: #ffffff;
     --color-charcoal: #1a1a1a;
     --color-muted: #6b7280;
     --color-border: #e5e5e5;
     --color-accent: #E91E63;

     --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;
     --font-mono: var(--font-geist-mono), ui-monospace, monospace;
   }

   html,
   body {
     background-color: var(--color-bg);
     color: var(--color-charcoal);
   }

   /* Pas de dark mode (D-11 — PRD strict) — aucun @media (prefers-color-scheme) */
   ```

   IMPORTANT (D-19) : aucun `tailwind.config.js`/`tailwind.config.ts` dans le repo. Si `pnpm create next-app` en a généré un, le supprimer (`rm tailwind.config.ts` si présent). La config Tailwind vit ENTIÈREMENT dans `app/globals.css` via `@theme`.

2. **Créer `.env.local.example`** à la racine de voodoo-hack/ (D-25, PRD lignes 482-488), contenu exact verbatim :
   ```
   # Server-only — never use NEXT_PUBLIC_ prefix for API keys
   SENSOR_TOWER_API_KEY=
   GEMINI_API_KEY=
   SCENARIO_API_KEY=

   # Public flag — controls dev mock mode (default: false). MUST be absent in production.
   NEXT_PUBLIC_USE_DEV_MOCKS=false
   ```

3. **Réécrire `README.md`** (le scaffold next-app en a généré un — le remplacer par un README minimal Next.js cohérent avec le projet, en français accepté) :
   ```markdown
   # Voodoo Creative Radar

   Outil interne Next.js qui transforme un signal marché live (Sensor Tower) en une publicité 30s testable (Scenario), via extraction de patterns créatifs gagnants par Gemini.

   ## Stack

   - Next.js 16.2 (App Router, Turbopack)
   - React 19.2
   - TypeScript 5.6+ strict (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`)
   - Tailwind CSS 4.1 zero-config (`@theme` dans `app/globals.css`)
   - Zustand 5 (state global, hydration-safe)
   - pnpm 9

   ## Setup

   ```bash
   cp .env.local.example .env.local
   # remplir les 3 clés API : SENSOR_TOWER_API_KEY, GEMINI_API_KEY, SCENARIO_API_KEY
   pnpm install
   pnpm dev
   ```

   ## Scripts

   - `pnpm dev` — démarre le serveur de développement (Turbopack)
   - `pnpm build` — build de production
   - `pnpm lint` — ESLint flat config
   - `pnpm start` — lance le build de production

   ## Deploy

   Vercel (auto-détection Next.js). Variables d'env à configurer dans **Production + Preview + Development**. `NEXT_PUBLIC_USE_DEV_MOCKS` doit être absent en Production.

   ## Documentation

   - `voodoo-creative-radar-prd.md` — PRD source
   - `.planning/PROJECT.md` — vision + contraintes
   - `.planning/ROADMAP.md` — 10 phases du build
   - `CLAUDE.md` — guide pour Claude Code

   ## Sandbox archivée

   L'app Vite préliminaire a été archivée sous le tag git `vite-sandbox-final`. `git checkout vite-sandbox-final` pour y revenir.
   ```
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q '@import "tailwindcss"' app/globals.css && grep -q '#E91E63' app/globals.css && grep -q "color-charcoal: #1a1a1a" app/globals.css && grep -q "color-bg: #fafafa" app/globals.css && test ! -f tailwind.config.ts && test ! -f tailwind.config.js && grep -q "SENSOR_TOWER_API_KEY=" .env.local.example && grep -q "GEMINI_API_KEY=" .env.local.example && grep -q "SCENARIO_API_KEY=" .env.local.example && grep -q "NEXT_PUBLIC_USE_DEV_MOCKS=false" .env.local.example && grep -q "pnpm dev" README.md && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@import "tailwindcss"' app/globals.css` exit 0
    - `grep -q "@theme" app/globals.css` exit 0
    - `grep -q -- "--color-bg: #fafafa" app/globals.css` exit 0
    - `grep -q -- "--color-surface: #ffffff" app/globals.css` exit 0
    - `grep -q -- "--color-charcoal: #1a1a1a" app/globals.css` exit 0
    - `grep -q -- "--color-muted: #6b7280" app/globals.css` exit 0
    - `grep -q -- "--color-border: #e5e5e5" app/globals.css` exit 0
    - `grep -q -- "--color-accent: #E91E63" app/globals.css` exit 0
    - `test ! -f tailwind.config.ts && test ! -f tailwind.config.js` exit 0 (D-19 : pas de config file)
    - `.env.local.example` contient les 4 lignes exactes : `SENSOR_TOWER_API_KEY=`, `GEMINI_API_KEY=`, `SCENARIO_API_KEY=`, `NEXT_PUBLIC_USE_DEV_MOCKS=false`
    - `git check-ignore .env.local` exit 0 (vérifie que .env.local serait ignoré s'il existait)
    - `README.md` contient les sections Stack, Setup, Scripts, Deploy
    - `grep -q "vite-sandbox-final" README.md` exit 0
    - `pnpm build` exit 0
  </acceptance_criteria>
  <done>app/globals.css réécrit avec @import "tailwindcss" + @theme tokens (6 couleurs PRD + font tokens Geist) ; aucun tailwind.config.* file présent ; .env.local.example shippé avec les 4 variables PRD valeurs vides ; README.md minimal Next.js cohérent avec mention du tag vite-sandbox-final ; pnpm build passe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disque local → Repo git | Risque de commit accidentel d'un `.env.local` avec secrets |
| Build Next.js → Bundle client | Risque de leak `process.env.SENSOR_TOWER_API_KEY` dans le bundle si le préfixe `NEXT_PUBLIC_` était utilisé par erreur sur une clé secrète |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.local` (secrets API) | mitigate | `.gitignore` étend `.env*.local` (Task 1) ; seul `.env.local.example` (vide) est commité ; vérification : `git ls-files \| grep -E "\\.env" \| grep -v example` doit ne rien retourner |
| T-01-02 | Information Disclosure | Variables d'env exposées au bundle client | mitigate | Convention strict : seul `NEXT_PUBLIC_USE_DEV_MOCKS` porte le préfixe `NEXT_PUBLIC_` (D-25) ; les 3 clés API restent server-side (validé en Plan 02 via `import 'server-only'` sur lib/{sensorTower,gemini,scenario,prompts}.ts) |
| T-01-03 | Tampering | Suppression accidentelle de l'historique Vite | mitigate | Tag git `vite-sandbox-final` créé AVANT toute suppression (Task 1, étape 2 stricte avant étape 4) ; rollback possible via `git checkout vite-sandbox-final` |
| T-01-04 | Information Disclosure | README.md ou docs publiques mentionnant des clés | accept | README ne contient aucun secret, juste des noms de variables ; risque résiduel nul |
| T-01-05 | Repudiation | Modification non-tracée de l'arborescence | accept | Repo git versionne tous les changements ; un seul mainteneur (solo dev), pas de besoin RBAC |
</threat_model>

<verification>
Verification automatisée à la fin du Plan 01 :

1. `git tag -l vite-sandbox-final` retourne le tag
2. `pnpm install && pnpm build` exit 0
3. `pnpm dev` démarre sans erreur (test manuel : ouvrir http://localhost:3000, voir la page Next.js par défaut — sera remplacée en Plan 04)
4. `tsc --noEmit` exit 0 (vérifie strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes)
5. Pas de fichiers Vite résiduels : `find . -maxdepth 2 -name "vite.config.*" -o -name "index.html" -not -path "./node_modules/*"` retourne vide
6. `.env*` git-ignoré : `git check-ignore .env.local` exit 0
7. Aucun secret commité : `git ls-files | grep -E "\\.env" | grep -v example` retourne vide
</verification>

<success_criteria>
- [ ] Tag `vite-sandbox-final` existe et pointe sur le dernier commit Vite
- [ ] `pnpm dev` démarre Next.js 16.2.x sans erreur
- [ ] `pnpm build` compile sans erreur TypeScript
- [ ] tsconfig.json a `strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes`
- [ ] app/globals.css : `@import "tailwindcss"` + `@theme` avec les 6 tokens couleurs + tokens font
- [ ] Pas de `tailwind.config.*` (zero-config Tailwind v4)
- [ ] `.env.local.example` shippé avec 4 vars PRD vides ; `.env*` git-ignoré
- [ ] Arborescence PRD complète : `app/project/`, `components/{layout,landing,game,market,patterns,creative,ui}/`, `lib/`, `prompts/`, `data/`
- [ ] README.md minimal cohérent avec stack Next.js + mention tag vite-sandbox-final
- [ ] Aucun secret commité dans l'historique
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-landing/01-01-SUMMARY.md` documenting:
- Versions exactes installées (Next.js, React, Tailwind, Zustand)
- Confirmation du tag `vite-sandbox-final`
- Liste des fichiers Vite supprimés
- Output de `pnpm build` (succès)
- Tokens `@theme` posés (les 6 couleurs + 2 fonts)
- Conventions à suivre par les Plans 02-04 : aucun tailwind.config, alias `@/` pour imports, tsconfig strict
</output>
