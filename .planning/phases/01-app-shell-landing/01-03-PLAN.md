---
phase: 01-app-shell-landing
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/ui/Button.tsx
  - components/ui/Card.tsx
  - components/ui/Badge.tsx
  - components/ui/Select.tsx
  - components/ui/Input.tsx
  - components/ui/Textarea.tsx
  - components/ui/Tabs.tsx
  - components/ui/LoadingState.tsx
  - components/ui/ErrorState.tsx
  - components/ui/EmptyState.tsx
autonomous: true
requirements:
  - REQ-file-architecture
user_setup: []
must_haves:
  truths:
    - "Les 10 atomes UI exportent des composants typés strict (pas de any) consommables par les Plans 04+ et toutes les phases ultérieures"
    - "Tous les atomes utilisent les tokens design system via `var(--color-*)` ou classes Tailwind générées par @theme"
    - "Aucun atome n'utilise dark mode ; aucun atome ne porte du magenta sauf Button variant=primary, Tabs active state, et focus rings"
    - "Tabs et tout composant interactif a `'use client'` ; les autres restent server-component-compatible"
    - "tsc --noEmit + pnpm build passent"
  artifacts:
    - path: components/ui/Button.tsx
      provides: "Button avec variants primary/ghost/outline + sizes sm/md/lg, supporte tous les attributs HTML button"
      contains: "variant"
    - path: components/ui/Card.tsx
      provides: "Card conteneur rounded-xl avec props optionnelles title + description"
      contains: "rounded-xl"
    - path: components/ui/Badge.tsx
      provides: "Badge inline avec variants neutral/accent"
      contains: "rounded-full"
    - path: components/ui/Select.tsx
      provides: "Select natif HTML5 avec label optionnel"
      contains: "<select"
    - path: components/ui/Input.tsx
      provides: "Input natif HTML5 avec label optionnel"
      contains: "<input"
    - path: components/ui/Textarea.tsx
      provides: "Textarea natif HTML5 avec label optionnel"
      contains: "<textarea"
    - path: components/ui/Tabs.tsx
      provides: "Tabs controlled (value/onChange), client component"
      contains: "'use client'"
    - path: components/ui/LoadingState.tsx
      provides: "Loader2 spinner + texte de chargement (text prop)"
      contains: "animate-spin"
    - path: components/ui/ErrorState.tsx
      provides: "Bloc erreur avec message + bouton Retry optionnel"
      contains: "onRetry"
    - path: components/ui/EmptyState.tsx
      provides: "Placeholder pour state vide (consommé par Phase 2 tabs)"
      contains: "EmptyState"
  key_links:
    - from: "components/ui/*.tsx"
      to: "lib/utils.ts (cn helper)"
      via: "import { cn } from '@/lib/utils'"
      pattern: "from '@/lib/utils'"
    - from: "components/ui/LoadingState.tsx"
      to: "lucide-react Loader2"
      via: "import { Loader2 } from 'lucide-react'"
      pattern: "Loader2"
---

<objective>
Construire les 10 **atomes UI** de `components/ui/` qui seront consommés par toutes les phases (Plans 04 de Phase 1 + tous les composants steps des Phases 3-9). Skin minimal mais API complète et stable — D-23 : Phase 1 stylise ce que la landing + le shell `/project` consomment, mais l'API reste figée pour les phases ultérieures.

Purpose: Sans ces atomes, le Plan 04 ne peut pas construire la landing ni le shell `/project`. Les atomes consomment les tokens design system de Plan 01 (`@theme` dans `app/globals.css`) via `var(--color-*)` ou utilities Tailwind v4.
Output: 10 fichiers `.tsx` typés strict, exportant des composants React 19 compatibles server/client selon leur nature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-app-shell-landing/01-CONTEXT.md
@.planning/phases/01-app-shell-landing/01-PATTERNS.md
@.planning/phases/01-app-shell-landing/01-01-SUMMARY.md

<interfaces>
<!-- Helper cn() exposé par lib/utils.ts (Plan 02 Task 1 — disponible quand ce plan s'exécute si Plan 02 finit avant Plan 03 dans la même Wave 2 ; sinon impl inline du fallback). -->
<!-- IMPORTANT : Plan 02 et Plan 03 sont en parallèle Wave 2 — pas d'overlap files_modified. -->
<!-- Si lib/utils.ts n'existe pas encore au moment de l'exécution (race), créer un fallback local minimal puis Plan 02 fournira l'impl finale (mais pnpm build s'attend à ce que lib/utils.ts existe). -->
<!-- Décision : ce plan IMPORTE { cn } depuis '@/lib/utils' SANS le créer ; si Plan 02 n'a pas encore livré lib/utils.ts au moment du build, l'executor crée lib/utils.ts en parallèle avec le contenu défini dans Plan 02 Task 1 step 4 (3-line file, sans risque de conflit puisque Plan 02 ne touche pas components/ui/ et Plan 03 ne touche pas le reste de lib/). -->

Tokens disponibles depuis app/globals.css @theme (Plan 01 Task 3) :
  --color-bg #fafafa
  --color-surface #ffffff
  --color-charcoal #1a1a1a
  --color-muted #6b7280
  --color-border #e5e5e5
  --color-accent #E91E63

Pattern d'utilisation Tailwind v4 :
  - Variables utilisables via `bg-[--color-accent]`, `text-[--color-charcoal]`, etc.
  - Avec @theme, des utilities natives sont aussi générées : `bg-bg`, `text-charcoal`, `border-border` etc. — préférer la syntaxe `[--color-*]` pour clarté explicite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Atomes form & layout — Button, Card, Badge, Select, Input, Textarea</name>
  <read_first>
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (sections Button, Card, Badge, Select, Input, Textarea — patterns analogs)
    - app/globals.css (tokens @theme posés par Plan 01 Task 3)
    - lib/utils.ts (cn helper — créé par Plan 02 Task 1, ou créé ici si race condition)
  </read_first>
  <files>
    - components/ui/Button.tsx
    - components/ui/Card.tsx
    - components/ui/Badge.tsx
    - components/ui/Select.tsx
    - components/ui/Input.tsx
    - components/ui/Textarea.tsx
  </files>
  <action>
**Convention pour tous les atomes :**
- Pas de `'use client'` (sauf si interaction stateful — pas le cas pour ces 6 atomes, ils sont contrôlés par le parent)
- TypeScript strict : interfaces explicites étendant les props HTML natifs (`React.ButtonHTMLAttributes<HTMLButtonElement>`, etc.)
- Utiliser `cn()` depuis `@/lib/utils` pour merger les classes
- `forwardRef` si nécessaire pour l'accessibilité (pas obligatoire en Phase 1, on peut s'en passer)
- Tokens consommés via `bg-[--color-accent]`, `text-[--color-charcoal]`, etc.

Si `lib/utils.ts` n'existe pas encore (Plan 02 pas terminé), créer le fichier avec ce contenu exact (3 lignes — Plan 02 fournit le même contenu, pas de conflit) :
```typescript
// lib/utils.ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]): string { return twMerge(clsx(inputs)); }
```

**1. `components/ui/Button.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'ghost' | 'outline';
  size?: 'sm' | 'md' | 'lg';
}

export function Button({
  variant = 'primary',
  size = 'md',
  className,
  ...props
}: ButtonProps) {
  return (
    <button
      className={cn(
        'inline-flex items-center justify-center gap-2 rounded-md font-medium transition-colors',
        'focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent] focus-visible:ring-offset-2',
        'disabled:opacity-50 disabled:cursor-not-allowed',
        variant === 'primary' &&
          'bg-[--color-accent] text-white hover:opacity-90',
        variant === 'ghost' &&
          'text-[--color-charcoal] hover:bg-[--color-border]',
        variant === 'outline' &&
          'border border-[--color-border] bg-[--color-surface] text-[--color-charcoal] hover:bg-[--color-border]',
        size === 'sm' && 'px-3 py-1.5 text-sm',
        size === 'md' && 'px-4 py-2 text-sm',
        size === 'lg' && 'px-6 py-3 text-base',
        className,
      )}
      {...props}
    />
  );
}
```

**2. `components/ui/Card.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  title?: string;
  description?: string;
}

export function Card({
  title,
  description,
  children,
  className,
  ...props
}: CardProps) {
  return (
    <div
      className={cn(
        'rounded-xl border border-[--color-border] bg-[--color-surface] p-6 shadow-sm',
        className,
      )}
      {...props}
    >
      {title && (
        <h2 className="text-base font-semibold text-[--color-charcoal] mb-1">
          {title}
        </h2>
      )}
      {description && (
        <p className="text-sm text-[--color-muted] mb-4">{description}</p>
      )}
      {children}
    </div>
  );
}
```

**3. `components/ui/Badge.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'neutral' | 'accent';
}

export function Badge({
  variant = 'neutral',
  className,
  ...props
}: BadgeProps) {
  return (
    <span
      className={cn(
        'inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium',
        variant === 'neutral' &&
          'bg-[--color-border] text-[--color-charcoal]',
        variant === 'accent' &&
          'bg-[--color-accent] text-white',
        className,
      )}
      {...props}
    />
  );
}
```

**4. `components/ui/Select.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface SelectProps extends React.SelectHTMLAttributes<HTMLSelectElement> {
  label?: string;
}

export function Select({ label, className, children, id, ...props }: SelectProps) {
  return (
    <label className="flex flex-col gap-1 text-sm">
      {label && <span className="text-[--color-muted]">{label}</span>}
      <select
        id={id}
        className={cn(
          'rounded-md border border-[--color-border] bg-[--color-surface]',
          'px-3 py-2 text-sm text-[--color-charcoal]',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent]',
          'disabled:opacity-50 disabled:cursor-not-allowed',
          className,
        )}
        {...props}
      >
        {children}
      </select>
    </label>
  );
}
```

**5. `components/ui/Input.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label?: string;
}

export function Input({ label, className, id, ...props }: InputProps) {
  return (
    <label className="flex flex-col gap-1 text-sm">
      {label && <span className="text-[--color-muted]">{label}</span>}
      <input
        id={id}
        className={cn(
          'rounded-md border border-[--color-border] bg-[--color-surface]',
          'px-3 py-2 text-sm text-[--color-charcoal]',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent]',
          'disabled:opacity-50 disabled:cursor-not-allowed',
          className,
        )}
        {...props}
      />
    </label>
  );
}
```

**6. `components/ui/Textarea.tsx`** :
```typescript
import { cn } from '@/lib/utils';

export interface TextareaProps extends React.TextareaHTMLAttributes<HTMLTextAreaElement> {
  label?: string;
}

export function Textarea({ label, className, id, ...props }: TextareaProps) {
  return (
    <label className="flex flex-col gap-1 text-sm">
      {label && <span className="text-[--color-muted]">{label}</span>}
      <textarea
        id={id}
        className={cn(
          'rounded-md border border-[--color-border] bg-[--color-surface]',
          'px-3 py-2 text-sm text-[--color-charcoal]',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent]',
          'disabled:opacity-50 disabled:cursor-not-allowed',
          className,
        )}
        {...props}
      />
    </label>
  );
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && grep -q "variant.*primary.*ghost.*outline" components/ui/Button.tsx && grep -q "rounded-xl" components/ui/Card.tsx && grep -q "rounded-full" components/ui/Badge.tsx && grep -q "<select" components/ui/Select.tsx && grep -q "<input" components/ui/Input.tsx && grep -q "<textarea" components/ui/Textarea.tsx && grep -q "from '@/lib/utils'" components/ui/Button.tsx && grep -L "'use client'" components/ui/Button.tsx components/ui/Card.tsx components/ui/Badge.tsx components/ui/Select.tsx components/ui/Input.tsx components/ui/Textarea.tsx | wc -l | grep -q "^6$" && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 6 fichiers existent : `test -f components/ui/Button.tsx components/ui/Card.tsx components/ui/Badge.tsx components/ui/Select.tsx components/ui/Input.tsx components/ui/Textarea.tsx`
    - Aucun de ces 6 fichiers n'a `'use client'` : `grep -L "'use client'" components/ui/{Button,Card,Badge,Select,Input,Textarea}.tsx | wc -l` retourne 6
    - Tous importent `cn` depuis `@/lib/utils` : `grep -l "import { cn } from '@/lib/utils'" components/ui/{Button,Card,Badge,Select,Input,Textarea}.tsx | wc -l` retourne 6
    - Button : `grep -q "variant === 'primary'" components/ui/Button.tsx && grep -q "bg-\\[--color-accent\\]" components/ui/Button.tsx`
    - Card : `grep -q "rounded-xl border border-\\[--color-border\\]" components/ui/Card.tsx`
    - Badge : `grep -q "rounded-full" components/ui/Badge.tsx`
    - Select : `grep -q "<select" components/ui/Select.tsx && grep -q "label?: string" components/ui/Select.tsx`
    - Input : `grep -q "<input" components/ui/Input.tsx && grep -q "label?: string" components/ui/Input.tsx`
    - Textarea : `grep -q "<textarea" components/ui/Textarea.tsx && grep -q "label?: string" components/ui/Textarea.tsx`
    - Aucun composant ne contient `bg-neutral-` ou `text-neutral-` (anciens tokens dark mode Vite) : `grep -E "bg-neutral-|text-neutral-" components/ui/*.tsx` retourne vide
    - Aucun composant ne contient `bg-violet-` (ancien accent Vite) : `grep "bg-violet-" components/ui/*.tsx` retourne vide
    - `pnpm exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>6 atomes form/layout créés (Button, Card, Badge, Select, Input, Textarea) ; tous server-component-compatible (pas de 'use client') ; tous consomment `cn` depuis `@/lib/utils` et les tokens via `[--color-*]` ; aucun token dark mode résiduel ; tsc --noEmit passe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atomes feedback & navigation — Tabs, LoadingState, ErrorState, EmptyState</name>
  <read_first>
    - .planning/phases/01-app-shell-landing/01-PATTERNS.md (sections Tabs, LoadingState, ErrorState, EmptyState)
    - .planning/phases/01-app-shell-landing/01-CONTEXT.md (D-18 : WorkflowTabs.tsx est client ; D-23 EmptyState consommé en Phase 2)
    - lib/copy.ts (Plan 02 — pour le typage des messages loading)
  </read_first>
  <files>
    - components/ui/Tabs.tsx
    - components/ui/LoadingState.tsx
    - components/ui/ErrorState.tsx
    - components/ui/EmptyState.tsx
  </files>
  <action>
**1. `components/ui/Tabs.tsx`** — composant CONTROLLED (le parent gère l'état), client component obligatoire (D-18) car onClick handler :

```typescript
'use client';

import { cn } from '@/lib/utils';

export interface Tab {
  id: string;
  label: string;
}

export interface TabsProps {
  tabs: readonly Tab[];
  activeTab: string;
  onTabChange: (id: string) => void;
  className?: string;
}

export function Tabs({ tabs, activeTab, onTabChange, className }: TabsProps) {
  return (
    <div
      role="tablist"
      className={cn(
        'flex border-b border-[--color-border]',
        className,
      )}
    >
      {tabs.map((tab) => {
        const isActive = activeTab === tab.id;
        return (
          <button
            key={tab.id}
            role="tab"
            aria-selected={isActive}
            type="button"
            onClick={() => onTabChange(tab.id)}
            className={cn(
              '-mb-px border-b-2 px-4 py-3 text-sm font-medium transition-colors',
              'focus:outline-none focus-visible:ring-2 focus-visible:ring-[--color-accent]',
              isActive
                ? 'border-[--color-accent] text-[--color-accent]'
                : 'border-transparent text-[--color-muted] hover:text-[--color-charcoal]',
            )}
          >
            {tab.label}
          </button>
        );
      })}
    </div>
  );
}
```

**2. `components/ui/LoadingState.tsx`** — pattern Loader2 + texte (PATTERNS.md). Server-component-compatible (pas d'event handler) :

```typescript
import { Loader2 } from 'lucide-react';

import { cn } from '@/lib/utils';

export interface LoadingStateProps {
  text?: string;
  className?: string;
}

export function LoadingState({ text = 'Loading...', className }: LoadingStateProps) {
  return (
    <div
      role="status"
      aria-live="polite"
      className={cn(
        'flex items-center gap-3 text-[--color-muted]',
        className,
      )}
    >
      <Loader2 className="size-5 animate-spin text-[--color-accent]" aria-hidden="true" />
      <span className="text-sm">{text}</span>
    </div>
  );
}
```

**3. `components/ui/ErrorState.tsx`** — bloc erreur + Retry button. Si `onRetry` fourni, le composant devient interactif → `'use client'` requis (le bouton a un handler).

```typescript
'use client';

import { AlertCircle } from 'lucide-react';

import { cn } from '@/lib/utils';

export interface ErrorStateProps {
  message: string;
  onRetry?: () => void;
  className?: string;
}

export function ErrorState({ message, onRetry, className }: ErrorStateProps) {
  return (
    <div
      role="alert"
      className={cn(
        'flex items-start gap-3 rounded-md border border-red-200 bg-red-50 p-4',
        className,
      )}
    >
      <AlertCircle className="mt-0.5 size-5 shrink-0 text-red-600" aria-hidden="true" />
      <div className="flex flex-col gap-2">
        <p className="text-sm text-red-700">{message}</p>
        {onRetry && (
          <button
            type="button"
            onClick={onRetry}
            className="self-start text-sm font-medium text-red-600 underline hover:text-red-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-red-600"
          >
            Retry
          </button>
        )}
      </div>
    </div>
  );
}
```

**4. `components/ui/EmptyState.tsx`** — placeholder consommé en Phase 2 (D-23). Server-component-compatible :

```typescript
import type { ReactNode } from 'react';

import { cn } from '@/lib/utils';

export interface EmptyStateProps {
  title: string;
  description?: string;
  action?: ReactNode;
  className?: string;
}

export function EmptyState({ title, description, action, className }: EmptyStateProps) {
  return (
    <div
      className={cn(
        'flex flex-col items-center justify-center gap-2 rounded-xl border border-dashed border-[--color-border] bg-[--color-surface] p-12 text-center',
        className,
      )}
    >
      <h3 className="text-base font-semibold text-[--color-charcoal]">{title}</h3>
      {description && (
        <p className="text-sm text-[--color-muted]">{description}</p>
      )}
      {action && <div className="mt-2">{action}</div>}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd voodoo-hack && head -1 components/ui/Tabs.tsx | grep -q "'use client'" && head -1 components/ui/ErrorState.tsx | grep -q "'use client'" && grep -L "'use client'" components/ui/LoadingState.tsx components/ui/EmptyState.tsx | wc -l | grep -q "^2$" && grep -q "Loader2" components/ui/LoadingState.tsx && grep -q "animate-spin" components/ui/LoadingState.tsx && grep -q "AlertCircle" components/ui/ErrorState.tsx && grep -q "onRetry" components/ui/ErrorState.tsx && grep -q "role=\"tablist\"" components/ui/Tabs.tsx && grep -q "aria-selected" components/ui/Tabs.tsx && grep -q "border-dashed" components/ui/EmptyState.tsx && pnpm exec tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers existent : `test -f components/ui/{Tabs,LoadingState,ErrorState,EmptyState}.tsx`
    - Tabs.tsx ligne 1 = `'use client';` (D-18)
    - ErrorState.tsx ligne 1 = `'use client';` (a un handler onRetry)
    - LoadingState.tsx et EmptyState.tsx N'ONT PAS `'use client'` : `grep -L "'use client'" components/ui/{LoadingState,EmptyState}.tsx | wc -l` retourne 2
    - Tabs.tsx :
      - `grep -q "role=\"tablist\"" components/ui/Tabs.tsx` exit 0
      - `grep -q "role=\"tab\"" components/ui/Tabs.tsx` exit 0
      - `grep -q "aria-selected" components/ui/Tabs.tsx` exit 0
      - `grep -q "border-\\[--color-accent\\] text-\\[--color-accent\\]" components/ui/Tabs.tsx` exit 0 (active state magenta)
      - L'API exporte `tabs: readonly Tab[]` + `activeTab: string` + `onTabChange: (id: string) => void`
    - LoadingState.tsx :
      - `grep -q "import { Loader2 } from 'lucide-react'" components/ui/LoadingState.tsx` exit 0
      - `grep -q "animate-spin" components/ui/LoadingState.tsx` exit 0
      - `grep -q "text-\\[--color-accent\\]" components/ui/LoadingState.tsx` exit 0 (spinner color magenta)
      - `grep -q "role=\"status\"" components/ui/LoadingState.tsx` exit 0
      - `grep -q "aria-live=\"polite\"" components/ui/LoadingState.tsx` exit 0
    - ErrorState.tsx :
      - `grep -q "role=\"alert\"" components/ui/ErrorState.tsx` exit 0
      - `grep -q "onRetry" components/ui/ErrorState.tsx` exit 0
      - `grep -q "border-red-200" components/ui/ErrorState.tsx && grep -q "bg-red-50" components/ui/ErrorState.tsx` exit 0
      - Affiche le bouton uniquement si `onRetry` fourni : `grep -q "onRetry &&" components/ui/ErrorState.tsx` exit 0
    - EmptyState.tsx :
      - `grep -q "border-dashed" components/ui/EmptyState.tsx` exit 0 (style placeholder distinctif)
      - Props : `title: string` requis, `description?` et `action?` optionnels
    - Aucun token dark mode : `grep -E "bg-neutral-9|text-neutral-1" components/ui/*.tsx` retourne vide
    - `pnpm exec tsc --noEmit` exit 0
    - `pnpm build` exit 0
  </acceptance_criteria>
  <done>4 atomes feedback/navigation créés (Tabs client, LoadingState server, ErrorState client, EmptyState server) ; Tabs et ErrorState ont `'use client'` car ils contiennent des event handlers ; LoadingState consomme `Loader2` lucide-react + tokens magenta pour le spinner ; ErrorState affiche conditionnellement le bouton Retry ; EmptyState a un design dashed pour signaler "placeholder" ; tsc --noEmit + pnpm build passent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Props utilisateur (text, message, etc.) → DOM rendering | Risque XSS si du HTML était injecté en `innerHTML` |
| Click handler client → State applicatif | Risque DoS si un handler boucle ou throw une exception non gérée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Props string (`text`, `message`, `title`, `description`, `label`) rendues dans les atomes | mitigate | Tous les atomes utilisent React text rendering (`{text}`, `{children}`) — JAMAIS `dangerouslySetInnerHTML`. Le grep `dangerouslySetInnerHTML` sur components/ui/* doit retourner vide. React échappe automatiquement. |
| T-03-02 | DoS | Bouton Retry de ErrorState appelé en boucle | accept | Pattern user-facing — le user clique manuellement. Le handler `onRetry` est géré par le parent qui doit avoir sa propre logique de throttling (Phase 4+ avec Sensor Tower). En Phase 1, ErrorState est posé mais pas câblé à un appel API. |
| T-03-03 | Information Disclosure | Message d'erreur trop verbeux (stack trace, secret) injecté via prop `message` | mitigate | Convention documentée dans le SUMMARY : les phases ultérieures (4/6/9) doivent passer un message human-friendly à `ErrorState`, JAMAIS un raw error.message d'une API. À renforcer en Phase 10 (audit grep). |
| T-03-04 | Tampering | Tab `id` props non validé (collision deux onglets avec même id) | accept | Convention de typage : le parent contrôle les `tabs` (typé `readonly Tab[]`). Un duplicate id donnerait un comportement React key warning, sans risque sécurité. À documenter. |
</threat_model>

<verification>
1. `pnpm exec tsc --noEmit` exit 0
2. `pnpm build` exit 0
3. Composants client correctement marqués : Tabs.tsx + ErrorState.tsx ont `'use client'` ligne 1 ; les 8 autres ne l'ont pas
4. Aucun token dark mode (`bg-neutral-9*`, `text-neutral-1*`, `bg-violet-*`) dans components/ui/*
5. Magenta `[--color-accent]` utilisé UNIQUEMENT dans : Button (variant primary background), Tabs (active state border + text), focus rings, LoadingState spinner
6. Aucun `dangerouslySetInnerHTML` dans aucun atome
7. Tous les atomes consomment `cn` depuis `@/lib/utils`
</verification>

<success_criteria>
- [ ] 10 atomes UI créés dans components/ui/
- [ ] Tabs et ErrorState sont client components ('use client')
- [ ] Les 8 autres sont server-component-compatible
- [ ] Tous typés strict (interfaces explicites étendant les attributs HTML natifs)
- [ ] Tous consomment `cn` depuis `@/lib/utils`
- [ ] Tokens design system consommés via `[--color-*]` (pas de couleurs hardcodées sauf rouge erreur dans ErrorState)
- [ ] Aucun token dark mode (`bg-neutral-9*`, `text-neutral-1*`, `bg-violet-*`)
- [ ] Magenta limité aux endroits définis (Button primary, Tabs active, focus rings, LoadingState spinner)
- [ ] LoadingState consomme `Loader2` de lucide-react avec `animate-spin`
- [ ] ErrorState affiche conditionnellement le bouton Retry (si `onRetry` fourni)
- [ ] `pnpm exec tsc --noEmit` exit 0
- [ ] `pnpm build` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-landing/01-03-SUMMARY.md` documenting:
- Liste des 10 atomes avec leur server/client classification
- API publique (props) de chaque atome — pour servir de référence aux phases 3-9 qui les consomment
- Conventions de tokens utilisées (quand magenta vs charcoal vs muted)
- Pattern d'usage : `<Button variant="primary" size="md">`, `<Tabs tabs={TABS} activeTab={...} onTabChange={...} />`, etc.
</output>
